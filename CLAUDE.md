# Loom — Claude Code Project Briefing

## ⚡ Session Quick-Start

> Update this section at the end of every session. Full session-by-session history lives in the dated archives at `general_project_notes/notes/srtstitcher/CLAUDE*_ARCHIVE.md`.

**✅ 0.5.1 STAGED 2026-07-18 (version bumped, prod artifacts built, all committed to `monorepo-restructure`; Connor uploads to AMO + Chrome).** `apps/extension/package.json` 0.5.0→0.5.1; artifacts `apps/extension/.output/loomextension-0.5.1-{firefox,chrome}.zip` + full-monorepo `loom-source-0.5.1.zip` (`git archive HEAD` — never WXT's partial `*-sources.zip`). Reviewer/release copy: `SUBMISSION_0.5.1.md` (release 2269 chars ≤3000 ✓; reviewer 2487 ≤2500 ✓). **HEADLINE REVIEWER POINT: 0.5.1 adds NO new permissions and NO new hosts** — `wxt.config.ts` byte-identical to 0.4.0/0.5.0; everything is local UI/rendering or a bug fix in already-granted content scripts. Bundles 3 feature commits since 0.5.0: (1) `8729dd4` Netflix SPA-nav caption fixes (no-subs-until-F5 + sticky-subs; held-map manifest tracker + watch-left + identity guards — live-verified Chrome + Firefox; see the next entry); (2) `b5d1193` definition-card stable positioning + selectable text + resizable/persisted uniform scale; (3) `3c4f269` two independent caption line-spacing controls (multi-line line-height + annotation/romaji gap, per-platform, i18n ×12). tsc + **390 vitest** green; both prod builds green. **Owed before upload:** Connor's live smoke on the 0.5.1 dev builds (per `feedback_extension_caption_verification`) — the Netflix nav flows + the card resize/select + the two spacing sliders. **Deferred (NOT in 0.5.1):** ③ cross-cue grammar stitching (`surface_continuations` — server accepts it, client still doesn't send it).

**✅ FIXED 2026-07-18 (committed to `monorepo-restructure`, unreleased — rides 0.5.1) — TWO Netflix SPA-navigation bugs that live 0.5.0 has on BOTH browsers.** Connor reported on Chrome, then confirmed on Firefox — NOT browser-specific, just timing-dependent (which symptom shows depends on Netflix's manifest-parse-vs-`pushState` ordering). **Bug 1 "no subs until F5":** SPA-navigating browse→episode, the pill appeared but captions never loaded until refresh. Root cause = the MAIN-hook's SPA drop-gate DISCARDED any manifest that parsed before the URL flipped to its `/watch/<id>` (Netflix parses pre-`pushState` on a browse click, and never re-parses) → permanent starvation. **Bug 2 "sticky subs":** back-button→browse→open a DIFFERENT title rendered the PREVIOUS title's subs — leaving `/watch/` cleared nothing, so the stale `active`/`latestPayload`/ISO-latched-payload replayed onto the new media. **Fix (one architecture, all in `apps/extension`, no server change):** the manifest-tracker's single `pending` slot became a **bounded HELD-MAP keyed by movieId** (`manifest-tracker.ts`, rewritten pure reducer, 40 tests) — every manifest is HELD, never dropped, and ADOPTED only on an explicit title-naming signal (`reduceManifest` URL-match on fresh load · `reduceWatchChange` when the `/watch/` URL names a held title · URL-anchored `reduceMediaSwap` · NEW `reduceWatchLeft` clears active + cached tracklist on leaving `/watch/`). Plus identity guards on both the MAIN re-emit and the ISO latched-replay (`platform.currentVideoId()`, added to netflix + youtube), a 20 s labeled timeout on Netflix WebVTT fetches (a transient HK-edge CDN stall was hanging discovery — banned async-hang class), ISO payload-identity dedup (videoId+status+track ids+**signed URLs** → a fresh re-parse re-resolves, byte-identical re-emits skip), an `activeFromHeld` one-shot URL-refresh (aged held payload → expired-URL 403 self-heals on the next fresh parse), and **dev/prod `sessionStorage` activation-key namespacing** (`loom_activated_dev`, mirroring the postMessage channel split — side-by-side builds were cross-activating). **Live-verified by Connor across 3 nav cycles (ja/ko/zh-Hans) on Chrome + Firefox dev builds**; extensive `#seq t=ms`-stamped `[Loom NFLX MAIN]`/`[Loom ISO]` dev-only diagnostics added throughout the flow (invaluable, kept). Diagnosis + adversarial review ran as two multi-agent workflows (4-lens map, 3-lens+verify review — 9 confirmed findings all resolved or accepted-with-reason). Tests: manifest-tracker rewritten, **374 vitest green**, tsc clean. **NOT browser-specific → fixes both stores' 0.5.0 in the next release.** Detail in the **Step 5h** tripwire block below.

**Current state (end of 2026-06-27 session):** Steps 1–4 shipped (4f live; 4g Streamlit-deletion pending). Step 5 (browser extension): **5a–5i shipped; v0.2.0 + v0.2.1 LIVE on AMO + Chrome (0.2.1 passed review ~2026-06-21); v0.2.2 STAGED this session — no-refresh SPA support on YouTube + Netflix, plus settings-panel clarity + Netflix platform-awareness (5h-5). Built + committed; Connor uploads.** The extension renders the full 4-layer stack on YouTube **and Netflix**: native (Bottom) + foreign (Top) + per-token annotation ruby (furigana / Pinyin / Zhuyin / Jyutping, auto-routed via `classifyLang` / Korean RR) + a **secondary full-utterance romanization line** covering every phonetic-layer language (CJK + Korean get both ruby and the line; Cyrillic / Thai / Indic / Hebrew / Arabic-Persian-Urdu get the line as their whole phonetic surface). Both layers fetch as a single batch POST on activation — `/annotate/batch` per (target_track, phonetic_system) and `/romanize/batch` per (track, phonetic_system, long_vowel_mode), ~3–4 s, then quiet. **Per-tab activation** (every YouTube tab starts dormant with a "Loom" pill; click to activate; `sessionStorage` persists across same-tab reloads) **plus a global on/off kill switch** (`lib/enabled.ts`, `browser.storage.local`, defaults on) surfaced as a popup toggle — when off, `LoomApp` renders nothing AND the background `webRequest` listener early-returns (zero timedtext observation, not just visual). The settings panel covers live track switching (manual/ASR badges + processing-tier indicators), per-layer `tlang` overrides, a 4-slot position picker, full per-layer styling (color + font + size + alpha + outline + glow for Bottom/Top/Annotation/Romanization), annotation + romanization toggles, alternate-orthography ruby (zh-Hant → Simplified) with Distinct/Merged tier highlights, 28 lang-aware color presets (from `/styles/presets`), an inline HSV color wheel (react-colorful), and a ☕ "Support Loom" link at the top (→ `/donate`). All persisted to `browser.storage.local`. **v0.2.0 settings-panel overhaul (shipped 2026-06-19):** the panel is now **four self-contained line-cards** (Bottom / Top / Annotation / Romanization), each owning its on/off, options, AND styling. New: per-line master **Show line** enable (Top off also hides its annotation / romanization / alt-orth — Loom doubles as a subtitle customizer); a **capability-driven phonetic-system picker** (`phoneticSystemsFor()` in `lib/captions/lang-support.ts` — Chinese / Thai / Arabic / Persian / Urdu systems, formerly CJK-only and the fix for "Thai paiboon not exposed"); language-gated controls (long-vowel → ja only, alt-orth → zh-Hant only); first-class per-line **opacity** with a linked Top-group (default linked; unlink toggle in Top's Advanced); dot-in-pill **Switch** toggles (greyed-gold off / neon-purple on) replacing the On/Off buttons; **pastel color defaults** (fresh installs) + Simplified-'same as Top'. Alt-orth Simplified ruby renders **ABOVE** the reading (prose corrected everywhere; language coding unchanged). Tracking doc: `apps/extension/UI_REVISIONS.md`. **The owner-key field is now dev-only** (gated behind `IS_DEV`, dead-code-eliminated from prod bundles — verified absent from `firefox-mv2` + `chrome-mv3`); the web app retains the Tier-A owner key. `logDev` is gated behind `IS_DEV` (prod ships quiet). **Extension at v0.2.1** (artifacts built 2026-06-22; Connor uploads to AMO + Chrome) — refinement release over 0.2.0: Netflix mid-episode track-switch fix (media-swap-anchored `manifest-tracker`), audio-language target default, smarter romanize/Pinyin defaults, outline + pill polish, collapsible settings, dev/prod channel namespacing. v0.2.0 was the FIRST public version with Netflix + the UI overhaul (submitted 2026-06-19). Prior public YouTube-only version was 0.1.7; AMO version numbers must be unique across channels. Reviewer field copy + Netflix MAIN-hook / permission justification: `SUBMISSION_0.2.1.md` (0.2.0's is `SUBMISSION_0.2.0.md`). **Build-time dev/prod split** ("Loom" vs "Loom (Dev)", distinct IDs, API base + verbosity via `lib/env.ts`). **Netflix port: FIRST PASS COMPLETE + live-verified (2026-06-19).** Same shared pipeline (overlay / annotate / romanize / settings / playhead) behind one `CaptionPlatform` impl + one MAIN entrypoint per site. Acquisition is a **MAIN-world `JSON.parse`/`JSON.stringify` manifest hook** (manifest is MSL-encrypted → not webRequest); each track carries its own signed WebVTT URL. Overlay anchors to `div[data-uia="player"]` (above Netflix's chrome). Text-vs-image is governed by **origin language** (ja/ko/zh/th/hi all serve WebVTT on native-origin content = the core learner case; image-only OCR is the off-axis foreign-translation case, gracefully degraded). Verified live on JJK (ja+furigana) + Crouching Tiger (zh-Hant+pinyin, EN-CC native), with furigana/romanize/presets served by the prod API. Detail in the **Step 5h** summary below + `NETFLIX_RECON.md` + `memory/reference_netflix_caption_acquisition_2026.md`. Production: `api.loom.nerv-analytic.ai` serves `/annotate/batch` + `/romanize/batch` + `/styles/presets`; web app live at `https://loom.nerv-analytic.ai` with `/privacy`, **`/support` (technical support/FAQ — the canonical store Support URL)**, and **`/donate` (PayPal + Venmo)**. Tests: **626** `test_` functions in `loom_core` (`tests/`) + **215** vitest cases (9 spec files) in `apps/extension`. _(static counts — re-run to confirm pass totals.)_

**✅ DECIDED 2026-07-11 — MOBILE STRATEGY + LOOM PLAYER plan of record (`MOBILE_ROADMAP.md`).** The mobile dream = local `.mkv` + tap-word gloss on phones. Sequencing Connor signed off: ① **`packages/player-ui`** — extract the extension's overlay/definition-card/settings stack into a host-agnostic WebView payload behind SEVEN seam interfaces (StorageAdapter / PlayerAdapter / PlayheadSource+ScaleSource / CaptionTrackSource / ApiConfig / LocaleProvider / host-side MountAdapter) — a full coupling audit ran this session (verdict: render/data stack mostly pure; deep couplings concentrated in `caption-context` ~55 storage calls, `discover.ts`, `stream.ts` playhead, `player-scale`/`use-paused`); ② **Loom Player desktop** = grow the EXISTING Tauri 2 shell + `tauri-plugin-libmpv` (prior art exists; libmpv owns A/V sync so the feared separate-clocks problem vanishes); ③ **Android** = Kotlin shell + `dev.jdtech.mpv:libmpv` Maven AAR + transparent WebView over the mpv surface + Media3 MatroskaExtractor for MKV sub-track demux (**ffmpeg-kit RETIRED Jan 2025 — don't plan on it**); ④ Chromecast custom Web Receiver as an Android output mode (receiver = Chrome page → overlay ports; codec wall applies; gloss goes second-screen on the phone); ⑤ iOS after Android. Independent cheap probe R1: **Firefox for Android runs AMO extensions** (open ecosystem since 2023-12, MV2 webRequest works) — live-recon YouTube acquisition there + `gecko_android` manifest key. Offline-gloss note: bundling a `dict.sqlite` subset in the app favors SQLite-on-R2 in the off-site dictionary-store decision (`DICTIONARY_SOURCES.md`). Substeps 7a–8c + R1 in `MOBILE_ROADMAP.md` §4; Connor signed off on the split. **7a ✅ + 7b ✅ SHIPPED same session** (commits `6fe249a` + the 7b pair): `@loom/player-ui` package extracted (i18n / annotate / romanize / orthography / captions-types / annotated-text + 8 test files; one-line re-export shims at old paths; extension vitest covers package tests) and ALL deep couplings rewired through the seams — `lib/host.ts` (browser LoomHost: storage/player/api, registered at module eval), `lib/host-dom/media-sources.ts` (all `<video>`/DOM plumbing: element-bound `acquirePlayhead` = old stream semantics, dynamic `pausedPlayhead` = old usePaused semantics, `scaleSource`), `stream.ts`/`use-paused`/`player-scale` now DOM-free seam subscribers, ~59 storage sites through StorageAdapter (only host.ts touches browser.storage), api-client through ApiConfig (X-Loom-Version wire-identical). LocaleProvider live (resolveUiLocale reads a registered provider). Byte-identical behavior by construction; 356 vitest + tsc + firefox/chrome builds green. **Owed: Connor live smoke test on the rebuilt dev build** (settings persist / per-platform size / collapse survive reload; activation; playhead tracking; pause→glow→card; kill switch) — then this is the next release's backbone. 7c residuals noted in the doc (define-lang browser.i18n, settings-panel BUILD_INFO + supportsTranslate). **NO server/API changes in any of this** (per Connor's explicit constraint). Also this session: **definition-pipeline survey over the new corpus batch** (Eva/Alice ja · Squid Game/All of Us Are Dead ko · RRR/Kalki/Dhurandhar hi, 30 tracks, ~101k tokens, live prod lookups): **useful% ja 97.3 · ko 93.3 · hi 89.5**; real gaps found = hi **nuqta normalization** (खत्म misses, ख़त्म hits — variant fallback in `_candidates` rescues the class), "alternative spelling of X" entries don't form-of-resolve (जिंदगी returns pointer text), KRDict has no profanity/slang (씨발 142× top ko miss; 깐부 misses), hi वाले (no form-of entry), bare numerals are clickable dead-ends. Survey harness in session scratchpad; DB access via the local Metabase container's stored DSN (its role can read corpus but NOT dictionary_entry → use the live API for lookups).

**🟢 BUILT + WORKING 2026-07-12 — LOOM PLAYER: SINGLE-WINDOW libmpv render engine (7c; Connor's in-app click-through owed).** **libmpv renders real video INSIDE the Tauri window with the DOM UI composited on top — one integrated window, VLC-style (live-verified, screenshot).** **KEY DECISION (Connor): `.ass` is NEVER the Player's core caption format** — no relative positioning → furigana alignment is server-side glyph-measurement guesswork (the reason `supports_ass_annotation` is CJK-only); generated `.ass` = test-scaffold + export only, the Player renders captions as **DOM (HTML `<ruby>`)**. **Architecture = single window** (`MOBILE_ROADMAP.md` §5a; a dual-window `--wid` MVP was built first, then SUPERSEDED): libmpv **render API → `GtkGLArea`** (video) with the transparent `WebKitWebView` (the `@loom/player-ui` DOM payload) composited over it in one `GtkOverlay`. Built: hand-written render-API FFI (`mpv_ffi.rs`; jammy libmpv 0.34.1/API 1.109, the `libmpv2` crate needs ≥0.35), `mpv_render.rs` (`player_attach` reparents the webview into the overlay, render context on the GLArea, property-pump re-emitting the same `mpv-prop` events → frontend PlayheadSource unchanged; `player_load`/`player_command`/`player_stop`), single-window `src/player-window/main.tsx`. Two unknowns spiked live first (transparent-webkit-over-GtkGLArea compositing; the FFI rendering into a GLArea). Three real bugs fixed in the smoke: forcing desktop GL segfaults libmpv on a GLES context (let GTK choose); React StrictMode double-mount frees mpv mid-setup → segfault (StrictMode off for the player window); overlay-above-vbox (3 levels) panics Tauri's edge-resize handler which hardcodes `webview.parent().parent()==Window` (pull webview out of vbox → 2 levels). `setlocale(LC_NUMERIC,"C")` after gtk init before `mpv_create`. **Second registered LoomHost** (localStorage / mpv PlayheadSource / PROD ApiConfig clientVersion null), local-file CaptionTrackSource on EXISTING sidecar routes (zero server changes), payload in `@loom/player-ui` (stream, auto-pick, api-client, annotate+romanize build-maps, capabilities, define-lang, definition-card, events-only subs parser, log hook). 362 vitest + desktop/extension tsc + cargo + extension builds green; NO server changes. **Owed:** Connor's click-through (open file → captions + pause→gloss over video); cleanup of the now-dead dual-window (`video_windows.rs`) + IPC (`mpv.rs`) Rust (superseded, still compiled). Wayland (no `--wid`/GLES subsurface) + Windows single-window-embedded are later tracks. _(The "furigana missing" ASS fix is moot for the Player — DOM captions — but kept for the generator.)_

**✅ LIVE ON BOTH STORES (confirmed 2026-07-11) — EXTENSION 0.5.0 submitted and available on Firefox AMO + Chrome Web Store.** _(Original staging notes kept below for the record.)_ Version bumped 0.4.0→0.5.0 (`chore` commit); prod artifacts built: `apps/extension/.output/loomextension-0.5.0-{firefox,chrome}.zip` + full-monorepo `loom-source-0.5.0.zip` (`git archive HEAD` — never WXT's partial `*-sources.zip`). Reviewer/release copy: `SUBMISSION_0.5.0.md` (release notes 2844 chars ≤3000 ✓; reviewer notes 5311, in line with prior). **The headline reviewer point: 0.5.0 adds NO new permissions and NO new hosts** — `wxt.config.ts` is byte-identical to 0.4.0; everything new is local UI or a richer response from the already-granted `api.loom.nerv-analytic.ai` origin. Bundles the 13 unreleased commits since 0.4.0: **capability-driven per-word dictionary lookup across ~20 languages** (not just ja/zh/ko — the `eef24f0` fix), **grammar breakdown card (JA + KO** — the card is language-generic so Korean renders for free), **in-card "Dictionary language" (gloss) picker + settings line**, **full UI i18n across 12 locales**, **single-line mode**, dictionary decoupled from the annotation toggle, default-collapsed settings + color-preset swatches. tsc clean, **356 vitest green**. Verified the dev-only owner-key UI field is DCE'd from prod (the `X-Loom-Auth` api-client middleware is unchanged from 0.4.0 + inert without a stored key). **NOT yet built into the client: ③ cross-cue grammar stitching** (`surface_continuations`) — server accepts it, client doesn't send it yet → deferred to 0.5.1 (Connor's call). **Owed before submit: Connor's live PROD smoke-test** (per `feedback_extension_caption_verification`) — per-word lookup lighting up on es/fr/de/hi/ru/ko, grammar card on a paused JA+KO word, the gloss picker, a non-English browser locale, single-line mode. No new caption-acquisition/platform code in this batch, so it's lower-risk than a platform release.

**✅ LIVE IN PROD 2026-07-11 — INTELLIGENT INFLECTION for ~15 languages via Wiktionary `form-of` resolution (Hindi was the ask; it generalized for free).** The task was "handle Hindi properly" — an inflected Hindi word (करते, लूँगा, घटनाओं) hit its own `dictionary_entry` row whose only gloss was a useless pointer ("inflection of करना"), so the card was a dead-end. **The insight that avoided a heavy NLP dependency:** kaikki Wiktextract ALREADY encodes the morphology — every inflected form is a `form-of` entry that (a) names its lemma in the gloss and (b) carries a structured grammatical `tags` list, and we were already storing those tags in the sense `misc`. So NO stanza/spaCy/PyTorch (~1 GB — wrong for the $5/mo box), NO per-language rule engine: one tag→feature map does it. **Two pure functions in `loom_core/grammar.py`:** `extract_form_of_lemma(gloss)` (text after the last " of ", strips "(translit)"/trailing colon/combining stress U+0301/U+0300) + `grammar_from_tags(tags, dict_form)` (drops `form-of`/unknown markers, orders voice→aspect→tense→mood→verbform→person→number→gender→case→politeness→degree via `_WIKT_TAG_ORDER`, humanizes via `_WIKT_TAG_DISPLAY`). **Wired into `/define/batch`** (`_form_of(defn)` detects a form-of first-sense; a SECOND batched `store.lookup` fetches the real lemma senses; the card returns the lemma's meaning + a `GrammarBreakdown` from the tags; `_to_grammar_model` bridges the dataclass→pydantic). **Verified live across the corpus words that used to dead-end:** hi करते→करना·habitual·participle·plural·masculine, लूँगा→लेना·future·1st-person·singular, घटनाओं→घटना·plural·oblique, किया→करना·perfective, बच्चों→बच्चा·plural·oblique. **And it lit up every other Wiktextract language with the SAME data shape at zero extra cost** — es comieron→comer·preterite·3rd·plural, fr mangé→manger·past·participle, de Kinder→Kind·plural·neuter, ru читал→читать·imperfective·past. This IS the "Romance/Slavic grammar leg" the #2 entry below predicted would need a morphological analyzer — it didn't; Wiktionary's editors already did the analysis. Commit `ef1dd3e`; tests `tests/test_form_of.py` (19). NO new dependency, NO extension release (rides the next build's card UI). Only ja/ko still use live morphology (MeCab/kiwipiepy) — everything else is dictionary-driven form-of.

**✅ LIVE IN PROD 2026-07-11 — SPEAKER-MARKUP for LATIN languages (#1) + KOREAN GRAMMAR breakdown (#2) (server-only backend push; client parts ride next build).** A corpus language survey (en 31k · ja 20k · zh 10k · de 3.3k · ko 3k · hi 2.8k · es 2.2k · fr 0.6k lines; measured conventions: es 13.6% bracket-label + 11.3% multi-speaker, fr 10.4%+12.0%, ko 10.7% multi-speaker, en heavy `[SFX]`) drove two high-yield expansions. **#1 Speaker-markup exclusion now reaches the LATIN generic token path** — ①/②'s label+dash drop only ran on the spans path (ja/zh/ko/hi/ru/uk); Latin generics (es/fr/de/it/pt/… ~14 langs) return codepoint-offset tokens with no spans, so a Spanish `(HOMBRE)` or French/English `- A\n- B` name stayed a clickable dead-end. New **`_drop_speaker_markup_tokens_cp`** drops tokens whose start codepoint falls in a marker (whole-cue `(Risas)` SFX kept). No client change (markup renders as loose non-interactive segments). **#2 Korean grammar breakdown (③ for ko)** — `grammar_supported` now `{ja, ko}`. Korean agglutinates endings onto a predicate stem like Japanese, and kiwipiepy already tags every morpheme while tokenizing for KRDict, so **`analyze_korean_grammar`** walks the same way: leading 안/못 negation, predicate head (VV/VA · noun+하/되 derived · noun+copula), then the ending chain — EP 았/었→past · 시→honorific · 겠→presumptive; 연결어미+보조용언 via lookahead (고 있다 progressive · 고 싶다 desiderative · 어 주다 benefactive · 어야 하다 obligation · 지 않다 negative · 을 수 있다 potential); EF mood/politeness (요→polite · ㅂ니다→formal · 어라→imperative · 자→propositive); copula 이다. dict form recovers the full 하다-verb (공부했어요→공부하다); continuation-stitching (③) works via a Korean word-boundary stop. Delivered through the existing `/define/batch` `grammar` field — no schema change, rides the next build alongside JA grammar. `ENGINE_VERSIONS`: Latin generics v2→3 (new drop); hi/ru/uk v3→4 (①/② span-drop had shipped without a bump — flushed now). Tests: +18 Korean (real kiwipiepy) +6 Latin-generic; **893 py green**. **Next grammar leg = Romance/Slavic** — ~~needs a real morphological analyzer~~ **SOLVED the cheap way** by the Wiktionary `form-of` work above (see the 2026-07-11 form-of entry): no analyzer needed, the dictionary already carries the tags.

**✅ LIVE IN PROD 2026-07-11 — MULTI-SPEAKER exclusion (②) + GRAMMAR cue-stitching (③) (corpus findings ②/③; server-only backend push, client parts ride next build).** The two remaining corpus findings, shipped together in one Railway push. **② Multi-speaker cues** (ZH 4.4%: `-[孫悟空]我拷！\n-[唐三藏]你…`): generalized ①'s leading-label exclusion to **ALL speaker-turn markers** — a turn dash + optional `[名]` label at the cue START or right after a `\n`. `strip_leading_speaker_label`→**`strip_speaker_markup`** (romaji routes use it); `_drop_leading_label_tokens`→**`_drop_speaker_markup_tokens`** (drops EVERY marker-covering word-token, not just the leading one). Display preserved (spans still reconstruct the whole cue incl. both names/dashes/newline); the names/dashes just aren't clickable dead-ends or romaji noise. Newline/BOS-anchored ONLY (no inline space-dash rule) so a space-padded content dash `3 - 5` is never stripped — the real corpus multi-speaker form is `\n`-separated anyway. **③ Predicate split across cues** (JA 9.2% end mid-grammar: `利用し`|`てタム…`): `analyze_japanese_grammar(surface, continuation="")` stitches the next cue's lead and **STOPS the suffix walk at the first new content word** (`_JA_WORD_BOUNDARY_POS`) → a split verb recovers its true inflection (`利用し`|`て`→`利用して`[te-form]; `食べ`|`ている`→progressive) **WITHOUT absorbing the next word's grammar** — the load-bearing case `食べ`|`て寝た` stays `食べる`[te-form], NOT [te-form, past]. A leading `（名）` on the continuation is stripped first; harmless when the surface is already complete (walk breaks immediately → identical to analyzing alone). Delivered via a new `/define/batch` request field **`surface_continuations`** (aligned to `words`); the client passes the next cue's lead for the last token (**rides next build**). `ENGINE_VERSIONS` ja→6 zh→5 yue→5 ko→4 (② changes tokens+romaji again vs ①'s v5). `@loom/api-client` regenerated. Tests: +8 speaker-label, +7 grammar; **869 py green**, extension tsc green. **Corpus findings ①/②/③ now all shipped** (mid-word-split ④ deliberately not built).

**✅ LIVE IN PROD 2026-07-11 — SPEAKER-LABEL exclusion (corpus finding ①; server-only, no extension build).** Ran a corpus-diagnosis harness (`scripts/diagnose_segmentation.py`, reads the live corpus read-only) over ~30k real JA+ZH cues (Frieren / Apothecary Diaries / Eva / Three-Body / C-dramas) to find where per-event analysis breaks. **Biggest phenomenon by far: a leading speaker-name / SFX label in brackets** — （フリーレン）, 【名】, [孫悟空], （戦闘音） — on **35.5% of JA cues and 12.4% of ZH**. It's hard-of-hearing metadata, not dialogue, and Loom was (a) tokenizing the name into a dead-end clickable word and (b) spelling it out on the romaji line. **Fix (`loom_core/romanize.py` + `loom_api/routes/romanize.py`): keep the label in DISPLAY** (annotation `spans` still reconstruct the full cue — honoring "Loom enhances information, not destroys it") **but exclude it from ANALYSIS.** `strip_leading_speaker_label()` + `_leading_label_span_count()` + `_drop_leading_label_tokens()`: `build_word_tokens` drops any word-token covering a leading-label span across ALL paths (ja/zh/ko/Brahmic-remapped) → those spans render as non-interactive "loose" segments client-side (`planWordGroups` already tolerates uncovered spans, **so ZERO client change** — ships live on deploy); the romaji routes strip the label before the phonetic line (cache keyed on stripped text so （A）行くよ/（B）行くよ/行くよ dedup); a whole-cue label （戦闘音） is left intact. Grammar is automatically clean (label never a clickable token → never sent to /define). `ENGINE_VERSIONS` ja→5 zh→4 yue→4 ko→3 (tokens + romaji output change → invalidate stale cache). **Verified live in prod** (（フリーレン）行くよ → romaji "Iku yo", not "(Furīren)iku yo"). Tests: +17 `test_speaker_labels.py` +2 romanize-batch = 855 py green. **The corpus also KILLED a tempting feature**: the earlier "mid-word split across cues" worry measured at a bogus 25.8% (a whitespace-offset bug in the detector); the corrected position-free detector puts the TRUE rate at **~0.2% JA / ~0.0% ZH** — blind cue-stitching would corrupt far more than it fixes (五四 countdowns, 您好/上将 coincidental bigrams, だなら merges), so it was **not built**. Remaining corpus findings for later, non-conflicting: **② multi-speaker dash-splitting** (`-[名]…\n-[名]…`, ZH 4.4%) and **③ incomplete→stitch for grammar only** (JA 9.2% cues end mid-grammar; gate hard on the incompleteness signal). Analysis + ranked proposals delivered this session.

**✅ SHIPPED 2026-07-08 — PER-WORD VOCAB LOOKUP (JA + ZH) is DONE and LIVE in prod; bundled into extension 0.4.0 (artifacts built, Connor uploads to AMO + Chrome).** See `VOCAB_LOOKUP.md` (Phases 0/1/2/2.1/2.2 all ✅). While paused, hover a Top-line JA/ZH word (soft drop-shadow **glow on the glyphs**, not a box) → click → a **definition card** shows reading (furigana/pinyin), **Hepburn romaji for JA** (`Tōkyō (Toukyou)`, bullet-separated from the furigana, doubled-form collapses when no long vowel), and dictionary senses. Backend: new **`/define/batch`** (NOT cached — one indexed query) over a unified `dictionary_entry` Postgres table (**JMdict** ja + **CC-CEDICT** zh, 502,656 rows, CC-BY-SA 4.0, attributed in-card + on `/privacy`). Robustness (corpus-measured over 114k JA tokens, 96% useful-result): **ZH dictionary-aware decomposition** (measure words 一顶→一+顶), **JA morpheme→word merging** (食べさせられた→食べる via the romaji merge_mask) + **contextual reading** (は→わ), **token punctuation-strip** (は…→は), **multi-key lookup** (lemma+surface, rescues 黒曜石), **JA honorific peel** (玉葉様→様, miss-gated so お母さん stays whole). **Deliberately NO JA deinflection ruleset** — MeCab's lemma already covers inflection; residual misses aren't deinflectable (`VOCAB_LOOKUP.md §8` analysis). Korean = Phase 3 (unrelated net-new; needs morphology dep + KR dict). Non-JA/ZH langs get NO interactivity (tokens empty → no dead-end glow). `ENGINE_VERSIONS` ja→4. Tests: `test_dictionary.py` (31) + `test_annotate_tokens.py` (16) + `test_ingest_dictionaries.py` (8) py; extension tsc + 290 vitest.

**✅ SHIPPED 2026-07-10 (unreleased — rides the next build) — DICTIONARY EXPANSION groundwork: capability-driven lookups + SINGLE-LINE mode.** Two client changes that make future dictionaries release-free and open Loom to single-track media. (1) **Server-declared dictionary capabilities** (`9989b7a`, `VOCAB_LOOKUP.md §6.1`): `GET /define/capabilities → {source_langs, gloss_langs, version}` (a lang appears only with BOTH a dictionary AND a tokenizer via `is_token_supported`); `/define/batch` gains `gloss_lang` (the language definitions are written in) with English fallback per-word; `dictionary_entry.gloss_lang` column. Client stops hardcoding definable langs — `capabilities.ts` session-caches the endpoint (fallback `{ja,zh}`), `define-lang.ts` only NORMALIZES codes (all Chinese→`zh`, never null), `isDefinable(caps,code)`/`resolveGlossLang()` drive interactivity. **So a new dictionary is a pure server ingest — no extension upload.** Deployed + verified live (`猫` `gloss_lang=fr`→English "cat"; caps `{ja,zh},{en},v1`). (2) **Single-line mode** (`pickPrimary` in `auto-pick.ts` + `discover.ts` resolution): media with only ONE track — a single foreign track OR all-native (English-only for an EN user, Chinese-only for a ZH user) — now activates instead of bailing `no-supported-track`. The one line is promoted into the TARGET role so it inherits styling + annotation + dictionary (dictionary lights up whenever the lang is definable). Guards: `targetIsPromotedNative` (no foreign track → no MT fabrication, no English→English MT) + `autoNativeDistinct` (don't draw the promoted track as Bottom too); the implicit-MT native fallback is now gated on `supportsTranslate` — **fixes a latent duplicate-line bug** where foreign-only on Netflix/Prime/iQIYI/WeTV (tlang ignored) refetched the same track and rendered it twice. Decisions (Connor): foreign-only keeps the auto-MT bottom line as default ON YOUTUBE (collapse via the Bottom "Show line" toggle, which already hides it visually); non-definable single lines STILL show ("Loom should be so beautiful users can't stand default subs"). The scouting survey for which dictionaries to build next (kaikki English Wiktextract X→English all langs; JMdict ja→9 langs; KRDict Korean→11 langs bulk-redistributable; **ZH↔JA has no open direct dict → pivot through English**) is in **`DICTIONARY_SOURCES.md`**. Tests: +18 vitest (capability normalize/gloss) +6 (`pickPrimary`) = 304 vitest; 36 py dictionary. **NOT yet in a store build** — bundles into the next release alongside 0.4.0's successor.

**✅ LIVE IN PROD 2026-07-10 — GENERIC LANGUAGE ENGINE + SPANISH (first non-CJK/Korean source).** The scalable path: any space-delimited language becomes definable with (a) the ONE generic tokenizer and (b) a Wiktextract dictionary — no per-language engineering. Architecture (custom-first dispatch, Connor signed off): `build_word_tokens` checks the bespoke analyzers (ja MeCab / zh jieba / ko kiwipiepy) FIRST; anything else with no custom path falls through to `_generic_tokens` (whitespace + **simplemma** lemmatization, ~50 langs, one light dep, fail-soft). Custom always wins (unit-tested); the generic path is **opt-in per language** (`GENERIC_TOKEN_PRIMARIES`) and only *definable* once its dictionary is ingested (capabilities intersects with `SELECT DISTINCT lang`). New dictionary source: **`parse_wiktextract`** over kaikki.org English Wiktextract JSONL (X→English, CC-BY-SA + GFDL; one row per word, IPA reading, form-of entries kept as a fallback). **The gate before enabling any language = `scripts/dict_quality_check.py`**: runs the PROD tokenizer over real text against a local dict export, reports lemmatized%/useful%/misses — never touches prod. **Spanish proven + shipped:** harness on 15k Tatoeba sentences vs Spanish Wiktextract (766k headwords) → **99.1% useful, every top miss a proper noun** (JA baseline ~96%); 804,263 es rows ingested to prod; verified live — capabilities lists `es`, `/define` es comer→"to eat"/casa→"house", and the full click path (comieron→lemma comer→"to eat"). `load_to_postgres` idempotency key changed to (source, lang) so one "wiktextract" source re-ingests one language without wiping others. dictionary_entry now ~1.9M rows (ja/ko/zh/es). Only remaining gap for users = the next extension build (annotate-route tokens for no-ruby langs + client rendering of plain clickable words). Tests: +5 generic-token, +6 Wiktextract parser.

**✅ LIVE IN PROD 2026-07-10 — TIER-1 BATCH: fr/de/it/pt/sv/nl added (10 definable source langs).** Ran `dict_quality_check.py` over ~15k Tatoeba sentences/lang against each kaikki Wiktextract headword set: **de 98.5% · pt 98.5% · sv 98.5% · nl 98.3% · it 97.0% · fr 94.4%** useful — all clear the ~96% JA bar, every residual miss a proper noun (John/Kyoto/Tatoeba) EXCEPT fr/it apostrophe elisions. So added a **Romance elision split** to the generic tokenizer (`_split_elision`): fr/it/ca/oc elide a proclitic before a vowel (l'école, d'un, j'ai) and the apostrophe is really a word boundary — peel a LEADING clitic only when ≤2 letters, so every elided clitic separates (école/ai/un now hit) while genuine apostrophe-lexemes stay whole (aujourd'hui, quelqu'un, presqu'île). `GENERIC_TOKEN_PRIMARIES += fr/de/it/pt/sv/nl`; `ENGINE_VERSIONS` es/fr/de/it/pt/sv/nl→2 (were [] tokens at default v1). Ingested to prod: fr 400,741 · de 367,157 · it 622,231 · pt 427,927 · sv 310,953 · nl 144,815 — **`dictionary_entry` now ≈ 4.2M rows / 10 source langs (de es fr it ja ko nl pt sv zh)**. Verified live: `is_token_supported ∩ SELECT DISTINCT lang` = all 10, and `/define` fr école→"school" · de essen→"to eat" · it/pt/sv/nl mangiare/comer/äta/eten all→"to eat". `/privacy` already credits "Other languages — Wiktionary via kaikki". Tests: +3 elision. **Adding the NEXT space-delimited language = run the harness, ingest if it passes, one line in `GENERIC_TOKEN_PRIMARIES` — no extension release.** Same remaining user-gap as the CJK/Korean work: the LIVE 0.4.0 hardcodes {ja,zh} client-side, so these light up only in the capability-driven next build.

**✅ LIVE IN PROD 2026-07-10 — TIER-2 BATCH: ru/pl/ro/da/cs/uk/hi/tr/id added (19 definable source langs) + Brahmic-safe tokenization.** Ran `dict_quality_check.py` over ~15k Tatoeba sentences/lang: **pl 98.6 · ru 98.2 · ro 97.8 · da 96.6 · cs 96.2 · uk 94.9 · tr 94.8 · id 94.0** — misses dominated by Tatoeba's Tom/Mary proper-noun flood, so real useful ~96%+. **The load-bearing fix: Brahmic tokenization.** Python's stdlib `\w`/`isalnum()` is FALSE for a combining mark, so the generic word regex dropped Devanagari dependent-vowel signs (matras) — करना truncated to करन, every Devanagari word missed the dict, Hindi measured 88.5%. Switched `_GENERIC_WORD_RE` to the **`regex` module's `\p{L}\p{M}`** (letters+marks) — fixes ALL Brahmic + Arabic/Hebrew scripts at once; stdlib fallback for Latin/Cyrillic if `regex` absent (added to `requirements.txt`). Hindi → 93.8% (real ~96% after Tom/Mary), misses now real words not fragments — and this unlocks every future Indic language. hi ALSO already has aksharamukha ruby → gets **ruby + clickable definitions** together; ru/uk get the Cyrillic romanization line + definitions. Ingested to prod: ru 441,543 · pl 194,149 · ro 130,161 · da 57,333 · cs 71,547 · uk 58,458 · hi 38,054 · tr 44,494 · id 38,450 — **`dictionary_entry` now ≈ 5.26M rows / 19 source langs, whole DB ~1.98 GB.** `GENERIC_TOKEN_PRIMARIES` += the 9; `ENGINE_VERSIONS` all→2. **Norwegian deferred** (kaikki ships it as macrolanguage `no`, which simplemma has no lemmatizer for — needs a no→nb map + harness pass). **tr/id ship with a known minor content-word gap** (Turkish İ-casing sentence-initial words; Indonesian enclitics -kah/-ku/-nya) — proper-noun-dominated otherwise, flagged for a future clitic-peel/casefold tweak. Tests: +2 (Devanagari matras in-word + offset; Cyrillic lemma). Same user-gap: rides the capability-driven next build.

**✅ LIVE IN PROD 2026-07-10 — GLOSS-AXIS FILL: English source + JMdict multi-gloss + CFDICT/HanDeDict (20 source langs · 16 gloss langs · 7.6M rows).** Working the gloss-language confusion matrix (rows = token language, cols = reader's language): before this, the ONLY filled column was English and the only filled row was Korean. Three low-hanging-fruit ingests (all pure server data, no extension release): (1) **English as a SOURCE language** — the largest learner audience couldn't click an English word; `en` added to `GENERIC_TOKEN_PRIMARIES`, harness on kaikki English Wiktextract (1.37M headwords) = **99.7% useful** (only `'s` possessives miss), 1,472,158 en→en rows ingested. (2) **JMdict multi-gloss** — we'd loaded only the English JMdict; `parse_jmdict` now auto-detects gloss_lang from each build's `languages` metadata (ger→de…), so the **Japanese row now has 9 gloss langs** (en·de·ru·nl·hu·es·fr·sv·sl, +567k rows) from the SAME source. (3) **CFDICT (zh→fr, 95k) + HanDeDict (zh→de, 265k)** — both CEDICT-format, so `parse_cedict` gained `source`/`gloss_lang` params; **Chinese row now en·fr·de**. Idempotency key became (source, lang, **gloss_lang**) so JMdict's per-file builds don't wipe each other. **Ingest hardening (load-bearing):** a German ingest stalled 27 min on a dead remote-proxy socket → switched `load_to_postgres` to psycopg **COPY** (183k rows: 27-min stall → **32 s**) + TCP keepalives + a (source,lang,gloss_lang) index for the DELETE. Verified live: caps 20 src/16 gloss; `/define` ja→de 食べる→essen, ja→ru→есть, zh→fr 猫→chat, zh→de→Katze, en→en cat. `dictionary_entry` now **7.6M rows / 2.77 GB**. Confusion-matrix analysis + the remaining lever (native-edition Wiktextracts = fill whole gloss COLUMNS) in the artifact + `DICTIONARY_SOURCES.md`. Tests: +6 parser (cedict source/gloss, jmdict gloss detect/auto). **Same client gap:** rides the capability-driven next build.

**✅ DECISION 2026-07-11 — OFF-SITE THE DICTIONARY DB (deferred; dictionary growth PAUSED).** The Railway Postgres volume is full at ~3.5 GB — the zh native edition (3.36M senses) + all further native columns don't fit. **Decision:** move `dictionary_entry` OFF the Railway operational Postgres onto a separate READ-ONLY store; pause dictionary growth until it exists. **Motivation:** (1) functionality is already massively expanded (20 source × 16 gloss, 8.95M rows, es+ja native columns live) — a strong stopping point; (2) finishing is now PURELY a backend/API change, no extension release — the client is capability-driven (`/define/capabilities`) and the server abstracts the store behind the `DictionaryStore` protocol, so an off-site backend is a drop-in (new impl + connection string, zero routes/client changes). **Rationale:** the dictionary is read-only reference data (~10 GB now, **15–25 GB at a full 30×30**) with the opposite profile to the small read-write cache/corpus — keeping it off the operational volume holds the $5/mo flat target. **Backend TBD:** Turso (hosted libSQL) is the cleanest drop-in but its ~8 GB free tier is likely too small for a 30×30 goal → paid Turso, a dedicated cheap Postgres (Neon/Supabase), or `dict.sqlite` on R2→container are the alternatives; choice DEFERRED pending 30×30 sizing. Full record + sizing in `DICTIONARY_SOURCES.md`. **State:** es+ja live; zh + fr/de/ru/… deferred; the `(source,lang,gloss_lang)` index was dropped to relieve the full disk (`_SCHEMA` recreates on next ingest).

**✅ LIVE IN PROD 2026-07-10 — NATIVE-EDITION WIKTEXTRACTS validated + first gloss column (es) live (lever 5).** The lever that fills a whole gloss COLUMN (definitions in one language across many source rows). On inspecting live `eswiktionary`/`jawiktionary` dumps, the native-edition JSONL is the SAME schema `parse_wiktextract` already handles — differs only in gloss LANGUAGE + many source langs per file. So NO parser variant: `parse_wiktextract` gained `gloss_lang` (stamp the column) + `keep_langs` (take only source langs we tokenize) + CLI `--wiktextract-gloss-lang`/`--wiktextract-keep-langs`. **es column harnessed vs Tatoeba** (the predicted "diagonal rich, cross thin" shape, measured): es→es **98.3%** (831k headwords, near the English edition's 99.1%), en→es 97.4%, fr→es 82.6%, pt 76.2%, it 72.4%, de 67.1% — cross-pair misses backstopped by the English column. Ingested the es column (911,704 rows: es 848k … zh 47); verified live `/define` gloss_lang=es → es comer→"Ingerir o tomar alimentos", en cat→"(Felis silvestris catus) Gato", fr chat→"Gato". `dictionary_entry` → **8.52M rows / 3.13 GB**. **The loop for the next column** (ja/zh next per `DICTIONARY_SOURCES.md`): download `kaikki.org/<xx>wiktionary/raw-wiktextract-data.jsonl` → ingest `--wiktextract-gloss-lang <xx> --wiktextract-keep-langs <set>`. No code, no extension release. Tests: +1 native-edition parser.

**✅ BUILT 2026-07-10 (backend; rides the next extension build) — KOREAN per-word lookup (Phase 3).** The first non-JA/ZH dictionary language, and the first use of the `gloss_lang` axis. Two parts (`VOCAB_LOOKUP.md §8 Phase 3`): (1) **Korean tokenizer** — `romanize.py::_korean_tokens` via **kiwipiepy** (chosen over mecab-ko/khaiii: pip-installable manylinux wheels, no system deps → clean on Railway; added to `requirements.txt`). Korean annotation spans are per-syllable (like Chinese); the analyzer groups them into words and recovers the **KRDict dictionary form** — predicates → stem + 다 (먹었어요 → 먹다; irregulars via kiwipiepy's normalized stem, 즐거워요 → 즐겁다), 하다/되다 derivations → the DERIVED form (공부했어요 → 공부하다, 깨끗하다 → 깨끗하다), nouns as-is. Load-bearing: irregular conjugations yield OVERLAPPING morpheme char-spans, so attachment tests "no whitespace between", not exact adjacency (`즐겁 [0,3)` + `어요 [2,4)`). `SUPPORTED_TOKEN_PRIMARIES += ko`, `ENGINE_VERSIONS["ko"]=2`. Live-validated against real kiwipiepy (+8 gated token tests). (2) **KRDict dictionary** — `ingest_dictionaries.py::parse_krdict` over **NIKL 한국어기초사전** LMF XML: Korean → up to **11 gloss langs** (en/ja/zh/fr/es/ar/mn/vi/th/id/ru), **CC-BY-SA 2.0 KR**, one `dictionary_entry` row per (headword, gloss_lang) so the `gloss_lang` axis surfaces them with NO client change; `capabilities()` will expose `ko` (has both tokenizer + data) + all gloss langs from `SELECT DISTINCT`. Media/audio URLs never read (not redistributable); `/privacy` + card attribution added. Parser validated against a real 35 MB mirror chunk (+8 parser tests). **✅ LIVE IN PROD 2026-07-10:** backend deployed (kiwipiepy installs, `is_token_supported("ko")` true), and the **official 11-language NIKL XML ingested** — **603,562 ko rows across all 11 gloss langs incl. Chinese** (`dictionary_entry` now 1,106,218 rows / ja+ko+zh). Verified live: `/define` ko 사람 → EN senses; `gloss_lang=ja` → JAPANESE senses (the axis works); `/annotate` ko → tokens 먹었어요→먹다. The official XML had TWO data bugs the parser now scrubs (unescaped `<` in a French gloss; a stray `\x08` control char in an Arabic gloss). Data lives in `dictionaries/krdict/` (gitignored). **ONLY remaining gap = the next extension store build** — the LIVE 0.4.0 hardcodes {ja,zh} client-side, so Korean isn't user-visible until the capability-driven client ships. py suite: +8 token, +10 KRDict parser.

**Prior design notes (now implemented) — PER-WORD VOCAB LOOKUP.** Turn Loom's caption stack into an interactive **per-word vocabulary lookup**: while the video is **paused**, hover a Top-line word (it glows) → click → a **definition card** anchored to the word shows its characters + annotation/romanization + dictionary senses. **Architecture SIGNED OFF: server-side, on-demand, cached** — own the dictionaries (**JMdict** JA / **CC-CEDICT** ZH / KR-dict TBD) and serve a new `/define` endpoint off the existing API, reusing the content-addressed result cache as a new `kind="definition"` keyed on the word/lemma. **No third-party live API** (and no new privacy boundary — the server already receives full subtitle text). Definitions are fetched per-click, NOT precomputed into the batch (payload bloat). **Key grounding finding that corrected the premise:** only **Japanese** annotation is genuinely word-level (MeCab — and the dictionary **lemma + POS are already computed at `romanize.py:919/926` then discarded**, just need exposing); **Chinese** annotation is **per-character**, BUT jieba word grouping **already exists** in `_make_pinyin_romanizer` (`romanize.py:635`, incl. the Trad→Simp map-back to surface slices at `:623-633`) — it's just collapsed to a string, so ZH only needs that `words` list **exposed** as tokens, no new segmentation and no lemmatizer (no inflection). **Korean** is **per-syllable** with no word grouping — the only language needing genuinely new segmentation (a morphological analyzer like mecab-ko/khaiii). Client tokens are already discrete DOM elements (`annotated-text.tsx:109-182`) so hover-glow/click bind per element. **Phasing: JA + ZH first** (clean + core audience), **Korean Phase 3** (hard leg — needs morphology dep + weaker dict data). Backend adds a word-level `tokens` grouping (surface + lemma + POS + sub-spans) alongside the existing flat `spans`, `engine_version` bump. Full spec + file refs + open decisions (card scope, `/define` vs `/define/batch`, KR dictionary source): **`VOCAB_LOOKUP.md`**.

**Parallel roadmap thrust — LOOM PLAYER (Path B — native desktop media player).** DECIDED 2026-07-01: build a branded, self-contained **Loom Player** — a native desktop app (Electron, or grow the existing **Tauri shell at `apps/desktop`**) wrapping a **native decoder (ffmpeg / libmpv)** so it plays **any local `.mkv`** — 10-bit HEVC/x265, AC3/EAC3/DTS/FLAC/TrueHD audio, ASS softsubs — and renders Loom's full 4-layer learning stack over it. **Why native, not a browser tab / extension:** the target audience is the **anime / fansub-torrent community** (who won't adopt a Netflix/streaming extension no matter how good Netflix's JP subs are — it's not their workflow or ethos), and their files are exactly what browsers CAN'T decode (10-bit HEVC + AC3/EAC3/DTS have no browser decoder, especially on Linux). A native decoder removes that codec wall entirely; a **browser-tab player was considered and REJECTED** for this reason. **Reuse (why Path B is tractable):** the extension's React **overlay / annotate / romanize** stack is all DOM → ports ~1:1 into an Electron/Tauri webview; Loom's engine **already generates the 4-layer `.ass`** (native + target + romanization + `\pos` furigana). Original anime ASS typesetting renders via **libass** (mpv-native) or **JASSUB** (libass-wasm) in a webview. **Audio: discrete 5.1/7.1 PCM** via native decode + multichannel output. **Atmos = stretch:** decode E-AC-3 JOC / TrueHD **OAMD objects** and render them OURSELVES to a channel bed or **binaural HRTF** (compelling for headphone users) — feasible per the "reverse-engineered Dolby Atmos using Claude" method (cf. `Theater-Simulator`: Electron + native ffmpeg OAMD parser). **Certified Atmos bitstream/passthrough is NOT possible** (Dolby crypto-gated encoder) and NOT needed — a software renderer outputs PCM to the user's own device. **Rejected alt: Path A (mpv Lua/JS script** injecting Loom's ASS via the API) — lower effort + plays everything, but Path B chosen for the branded, fully-controllable UX. Tailwind: **Fable re-authorization imminent.** Recon owed before building: pick Electron vs. grow-Tauri; confirm libmpv/ffmpeg embedding + A/V-sync approach (separate video + Web Audio clocks is the hard part); JASSUB vs native libass for original typesetting.

**Shipped 2026-07-07/08 — PRIME VIDEO port (platform #6) + POSITIONAL/VERTICAL/SIMULTANEOUS caption handling + a sizing/positioning settings round.** Governing principle Connor set for this arc: **"Loom enhances information, not destroys it for the purpose of processing it"** — preserve each cue's on-screen position + orientation instead of flattening everything into one slot.

- **Prime Video (`primevideo.com`) — sixth platform, live-verified on Evangelion 3.33.** Anime + films; JA/ZH/multi learner content. **Acquisition** = a MAIN-world `fetch`/XHR hook on `GetVodPlaybackResources` (plain JSON — NOT MSL-encrypted like Netflix); subtitles are **TTML2** on `cf-timedtext.aux.pv-cdn.net` (`entrypoints/prime-main.content.ts`, `lib/captions/prime/parse-ttml.ts`, `lib/captions/platform/prime.ts`, id `primevideo`). **Overlay anchor** = `.atvwebplayersdk-video-surface` (stable, video-sized, inside fullscreen). **Multi-surface gotcha:** Prime autoplays a ~30 s preview trailer on a full-size surface while the real ~6000 s episode spins up on a SEPARATE, near-identically-sized surface — disambiguated by video **DURATION, not area** (`resolvePrimePlayerSurface`; shared by the overlay anchor + playhead so they can't diverge). **Mount** = a STATE-BASED reconciler (`ensureMount`, NOT WXT `autoMount`) because Prime's 0×0→sized player transition is a layout change, not a DOM mutation; migrates on surface **identity** change. CORS: `primevideo.com` added to `loom_api/cors.py`. Recon evidence: `PLATFORM_RECON_PRIME_HBOMAX_ATV.md`.
- **Non-destructive layout parse.** New `CueLayout` (`writingMode` / `block` / `inline` / `regionId`) attached to `CaptionEvent` (`lib/captions/types.ts`). Prime TTML regions carry it (`横下` = horizontal-bottom, `縦右` = vertical-right; `tts:writingMode`/`origin`/`extent` → `parse-ttml.ts::parseRegions`/`resolveLayout`). Netflix WebVTT cue settings carry it too (`line:`/`position:`/`align:`/`vertical:` → `parse-vtt.ts::parseCueSettings`) — but **Netflix has NO vertical (image-based) and only `line:10%` top cues (~6% of a track), all NON-concurrent** (empirically: JJK S1E3 has bottom→top→bottom runs but they're sequential, never stacked).
- **Stream emits ALL concurrent cues** (`stream.ts::findActiveAll`); `pickPrimary` selects the horizontal-bottom cue as the main-slot primary and returns **null** when no horizontal cue exists (so a lone vertical cue stays vertical instead of being promoted into the horizontal main slot + romaji line — the bug fixed in `c475701`).
- **Overlay renders position + orientation.** The main top/bottom lines render through `renderZone`/the 4-slot picker as before; **non-default-zone extras render through `PositionalCue`** at their source zone + writing-mode (vertical ruby via `writing-mode: vertical-rl` + `<ruby>`; furigana only — no romaji line, no native pairing — the "ambient light" treatment Connor chose). **The extras path uses `zoneAnchor(layout)`, kept SEPARATE from the layout function's `zoneStyle`** — so signs / vertical cues keep their source position and the main-slot layout (and the new nudge sliders) never touch them. **v1 limitation (accepted):** the PRIMARY cue does NOT follow its source position — a lone Netflix `line:10%` cue renders in the user's slot (which, with the default split layout, is the top anyway). Faithful primary-follows-position was declined for Netflix since its top cues are non-concurrent; the extras path only fires where cues genuinely stack (Prime).
- **Passive pause probe** (`lib/overlay/caption-probe.ts`, dev-only): `installCaptionPauseProbe` on Prime + Netflix logs where each cue actually renders (zone / orientation / position% / font / inline runs) on pause or Ctrl+Shift+L — the render↔data correspondence tool; independent of activation.
- **Sizing/positioning settings round (uncommitted at time of writing; `UI_REVISIONS.md` 2026-07-08):** (1) **Picture-relative scaling** — `usePlayerScale` now measures the visible VIDEO PICTURE height (`<video>` intrinsic aspect, `object-fit: contain`) on ALL platforms, not the player container. Prime already looked right (its anchor IS the video box); Netflix/YT/WeTV/iQIYI resolved to larger containers so the shared 52/48 @1080 sizes rendered oversized — now consistent. (2) **"Subtitle size" knob PER PLATFORM** — one multiplier (50–150%, default 100) scaling the whole overlay uniformly, stored in a map keyed by `getPlatform().id`; does NOT change the per-line size numbers in the panel. (3) **Vertical nudge + line spacing PER PLATFORM** — top/bottom nudge (−40…+40% of player height, positive = toward picture center, for pulling text off letterbox bars) + line-spacing px, applied only to the main lines (`zoneStyle`), stored under one `{[platformId]:{top,bottom,spacing}}` map. (4) **Romanize LINE default → per SCRIPT FAMILY** (`discover.ts::targetRomanizeEnabledByFamily`): annotation-only for zh-Hans (Pinyin) / zh-Hant (alt-orthography) / Korean (RR) whose per-char ruby already gives the reading; romaji line kept for Japanese (kana ruby isn't a romanization). Was a single global boolean that leaked a Japanese choice onto Chinese/Korean; legacy boolean discarded on load.
- **Prime-remount persistence tripwire (load-bearing):** Prime's reconciler rebuilds the `CaptionProvider` 2–3× on a close→reopen (preview→episode surface migration), so a fresh mount at defaults racing the async `storage.local.get` made per-platform settings *appear* not to persist. Fixed by holding the per-platform size + position prefs in a **module-level cache read synchronously in the `useState` initializers** — every rebuild restores instantly; storage stays the source of truth. (Also fixed: the position keys were persisted but never added to the load-time `get([...])` list, so they never round-tripped.) Tests: **280** vitest (12 files).

**Shipped 2026-07-05 — corpus data pipeline is LIVE end-to-end (the R2 half that was owed since 2026-07-02).** Railway Postgres was wired (`DATABASE_URL` set on the API service) so BOTH the Layer-1 result cache and the Layer-2 corpus now persist in prod. Verified live: cache holds ~5k rows growing; corpus captures land from the **extension on YouTube + Netflix** (opt-in, `loom_corpus_opt_in:true` in `storage.local` — confirmed via a service-worker `chrome.storage.local.get` readout + a live Frieren reload). Cloudflare **R2 bucket `loom-corpus`** created; the **first real export ran end-to-end 2026-07-05** (`scripts/export_corpus.py --settle-days 0 --enrich`, run from a laptop against prod): **6 hive-partitioned Parquet files / 8,994 rows** (netflix en/ja/zh-Hant/zh-Hans + youtube zh/en) written to R2, `corpus_export_manifest` populated, all 20 source tracks stamped `archived_at`. The **monthly cron is wired + validated**: 6 GitHub repo secrets set (`CORPUS_DATABASE_URL`=Railway `DATABASE_PUBLIC_URL`, `CORPUS_BUCKET`, `CORPUS_S3_ENDPOINT`, `CORPUS_AWS_ACCESS_KEY_ID`, `CORPUS_AWS_SECRET_ACCESS_KEY`, `CORPUS_API_AUTH_KEY`), and a `dry_run` dispatch of `export-corpus` on `main` passed green (`enriched 0` / `wrote 0` — correct: 7-day settle window + already-archived). **Cron uses `main`'s copy of the workflow + scripts** — export-logic changes must reach `main` (not just `monorepo-restructure`) to take effect; next auto-run is Aug 1 06:17 UTC. Full validated runbook + the gotchas hit (bucket-before-token, Object R&W not read-only, PDF-print truncates 64-hex keys → `SignatureDoesNotMatch`, `DATABASE_PUBLIC_URL` not the internal URL) live in `CORPUS_WIRING.md §3`. **Two backlog items opened (`CORPUS_WIRING.md §7`, non-blocking):** (1) **extension capture has NO retry** — fire-and-forget, a down/disabled server drops that track forever (this is why the first Frieren episodes captured nothing: watched before `DATABASE_URL` was set → Null store → `reason:"corpus disabled"`, never retried); the desktop app already spools, the extension doesn't. (2) **Netflix `title` = "Netflix"** (cosmetic) — capture reads `document.title` which on a NF watch page is the literal "Netflix"; identity (`platform`+movieId) is correct, title is display-only, so no data corrupted; fix = read from manifest or `[data-uia="video-title"]`. **Owner cleanup still owed:** delete the dead/rotated R2 token + shred the desktop key PDF/PNG (keys now live in GitHub secrets + Railway).

**Shipped 2026-07-02 — romanization result cache Layer 1 (`ROMANIZATION_CACHE.md`).** `/romanize/batch` + `/annotate/batch` are now read-through/write-back against a content-addressed Postgres cache (`loom_api/result_cache.py`; keys = sha256 of kind/lang/RESOLVED-system/mode/engine_version/NFC-normalized text; annotate caches SPANS only — html re-renders per request so render_mode stays out of the key; repeated lines dedup in-batch even uncached). Fail-open by construction: no `DATABASE_URL` → `NullResultCache` (byte-identical to pre-cache), DB down → log + 30s backoff, cache built eagerly at worker boot (`web.py`) so init cost never lands on a user request. **Bump `ENGINE_VERSIONS` in `loom_core/romanize.py` whenever a romanizer's OUTPUT changes** — that's the whole invalidation story. Railway now runs **gunicorn + uvicorn-worker with `--max-requests 500`** (worker recycling = the RAM-creep relief, ROMANIZATION_CACHE.md §3–4 Option A). Telemetry: `loom.cache` INFO lines (hits/misses/rss_mb/langs_computed) = the §5 measurement. Tests: `tests/test_result_cache.py` (20). **DONE 2026-07-05: Railway Postgres wired + `DATABASE_URL` set on the API service** (kill switch `LOOM_RESULT_CACHE=off`); cache live in prod (~5k rows and growing). Watch hit rates via the `loom.cache` logs.

**Also shipped 2026-07-02 — corpus Layer 2 SERVER infrastructure (the media-identity training-data layer; Connor's stated primary interest).** `POST /corpus/capture` (mounted on web + main apps): opt-in-gated (`opt_in_training:false` → 200 stored=false — the long-owed 5f flag finally does something), fail-soft (down DB / no DSN → stored=false, never 500s the extension), idempotent (per-track `content_hash` dedup — rewatch = no-op, platform-revised track = new version). Storage: `loom_api/corpus_store.py` — `corpus_media`/`corpus_track`/`corpus_line`/`corpus_export_manifest`, shares the Layer-1 DSN + pool (`loom_api/db.py`), texts normalized with the SAME `normalize_text()` so export joins to `romanization_cache` are exact-match. **Corpus stores NO romanizations** — export joins them at read time (latest `engine_version` wins → romanizer fixes upgrade the exported corpus retroactively, no re-capture). Export: `loom_api/corpus_export.py` + `scripts/export_corpus.py` — Postgres landing zone → hive-partitioned Parquet (`corpus/platform=X/lang=Y/captured=YYYY-MM/`) on R2/S3, manifest-audited, `--dry-run`/`--local-dir`/`--prune`; monthly GH Action `export-corpus.yml` no-ops green until secrets exist. NO user/IP/install identity anywhere in the schema — media corpus, not user log. Tests: `tests/test_corpus.py` (21; InMemory-parity + pure record-shaping — the Postgres SQL's acceptance test is the first live run, per `feedback_extension_caption_verification` posture). `@loom/api-client` regenerated (+`/corpus/capture`; extension tsc + 220 vitest green). **Extension side (CORPUS_WIRING.md §1) IMPLEMENTED 2026-07-02** — `lib/corpus/consent.ts` (tri-state `loom_corpus_opt_in` + `loom_corpus_asked`, storage.onChanged-cached cross-context, **dev builds capture by default**, prod unset = off) + `lib/corpus/capture.ts` (pure payload builder mirroring the server's 422-able caps, session sent-set, fire-and-forget swallow-everything POST) wired into `discover.ts::resolveCaptions` alongside the batch fan-outs — **authentic tracks only** (tlang-MT layers + the implicit-native-MT fallback are skipped; MT is synthetic, not training data). **Consent design (revised after store-policy check, sources in the doc): two-touch ACTIVE ask, not a buried toggle** — `entrypoints/onboarding/` page (opened by `runtime.onInstalled` reason `install` ONLY; doubles as the parked first-run "What is Loom?" explainer) with primary "Contribute" button; `components/corpus-consent-prompt.tsx` one-shot re-ask on first `tracking` status (marks asked on SHOW, pill perf-tripwire-compliant: memoized, status-only subscription, no backdrop-filter, `swallowPlayerEvents`); settings-panel "Data" `Section` + `ToggleRow` as permanent control. **§1c CORRECTION: Firefox's native optional-data-permission route is unavailable** — the manifest already has `data_collection_permissions.required: ["websiteContent"]` (transmission IS core function; a category can't be required AND optional), so the onboarding UI is the consent surface on BOTH browsers. Tests: **237** vitest (11 files; +17 `lib/corpus/corpus.test.ts` — NOTE vitest runs as IS_DEV so unset consent = capture-on there). Silent default-on is a store-policy violation on both channels (Chrome affirmative-consent rule; AMO data-consent framework) — don't relitigate it. **Still owed:** §4 privacy/store copy (`/privacy` + listing — MUST ship in the same release as the extension corpus toggle). **DONE 2026-07-05 (see the "Shipped 2026-07-05" block above):** live capture verified (extension YouTube + Netflix → Postgres), Railway Postgres + `DATABASE_URL` (§2), R2 bucket + all 6 GH secrets + first export + cron dry-run (§3). Extension consent shipped in the LIVE 0.3.1 release (default-off, onboarding ask).

**Also shipped 2026-07-02 — MULTI-SURFACE corpus + cache (CORPUS_WIRING.md §6; decisions: web checkbox default-ON / style fields now / desktop→prod API).** Single `/romanize`+`/annotate` now share the batch endpoints' Layer-1 cache (web was bypassing it entirely). **Style capture** landed: `corpus_line.style` + `corpus_track.styles_json` (additive; `ALTER TABLE ADD COLUMN IF NOT EXISTS` migrates a live DB; style names hash into content identity, definitions don't; new Parquet columns) — the OCR `(text, style, language)` tuples; file sources capture ALL non-comment events incl. signs/karaoke (stylized text = the hard OCR case). **Web** (`apps/web`): `buildRomanizeMap` migrated to `/romanize/batch` (1 request/episode, was ~300 singles); `lib/api/corpus.ts` captures both parsed tracks post-parse (platform `web`, filename-stem + ffprobe-title identity, serialized SSAStyle map); default-ON per-run checkbox under Generate; **`/privacy` REWRITTEN** (Retention = the real cache; new "Training corpus (opt-in)" section covering extension + web — the §4 store-copy can point here; live on next Vercel deploy). **Desktop**: GenerateSection default-ON checkbox → the existing `opt_in_training` on `/generate/ass` → sidecar re-parses via the mtime cache and forwards capture payloads to the prod API (`loom_api/corpus_forward.py`, `LOOM_CORPUS_FORWARD_URL` default prod / `off` disables; stdlib urllib daemon thread; media identity = real filename stem via /files/by-path; native lang auto-detected). **Loom Player must include the same capture from day one** (platform `player` — richest source, full fansub ASS). **Desktop capture is OFFLINE-FIRST**: payloads spool to `~/.loom/corpus-spool/` (content-hash filenames, atomic writes) and flush at sidecar startup + post-generation — 2xx deletes, 4xx quarantines `.rejected.json`, network failure retries. **Clients ship raw text only; the server recomputes ALL derived data** (locally-computed romanizations never leave the desktop — cache-poisoning + engine-skew) via `export_corpus.py --enrich`: replays unarchived corpus texts through the batch endpoints pre-export (idempotent — hits are free; workflow passes `--enrich` by default, optional `CORPUS_API_AUTH_KEY` secret beats rate limits). Tests: 668 py / 237 vitest. Owed: prod verify rides the same §5 checklist; desktop spool→flush needs one live generation test once Postgres exists.

**✅ BUILT + LIVE IN PROD 2026-07-11 — GRAMMAR-AWARE BREAKDOWN of inflected words (Japanese v1).** The dictionary says what a word MEANS; this says what it's DOING. Click a paused word and the definition card now shows its inflection chain above the senses — **食べさせられた → 食べる (to eat) · causative · passive · past**. **The payoff is data we already compute and discard:** MeCab (fugashi+unidic-lite) tags every morpheme with POS + conjugation (`cForm`/`cType`) + lemma while producing furigana; `loom_core/grammar.py` walks that chain and maps the auxiliary/inflection sequence to ordered grammar features. **JA coverage:** causative, passive/potential (ambiguity surfaced, not hidden), desiderative, past, polite, negative (助動詞 ない AND adjective 無い), volitional + imperative (read off the head's `cForm`, no separate morpheme), te-form, the -te-aux aspectuals (progressive -te iru, completive -te shimau, preparatory/attemptive/benefactive/directional), ば/たら conditionals, copula; suru-verbs recover the full dict form (勉強しました → 勉強する · polite · past), na-adj+copula work, bare nouns/particles return None. **Delivery:** folded into `/define/batch` (per-click, no batch bloat) — new request field `surfaces` (inflected caption word vs lemma), new response `grammar {dict_form, features[{code, display, surface}]}` computed from the surface independent of the dict hit, fail-soft. Each feature carries a stable `code` (client-localizable later) + an English `display` shown now → **release-proof**. Client: definition-card renders a "Grammar" section (dict form → feature pills). One process-wide MeCab tagger now shared by the furigana pipeline + grammar analyzer (`get_shared_ja_tagger`). i18n: `define.grammar` × 12 locales (feature LABELS stay English in v1 — full label localization is a follow-up). **Korean (kiwipiepy) + Romance/Slavic are Phase 2 via the same `GrammarBreakdown` shape.** Deployed + verified live. Tests: +25 backend (`test_grammar.py`), 356 vitest + tsc green. **Client rides the next store build.**

**✅ FIXED + LIVE IN PROD 2026-07-11 — in-card "Dictionary language" picker + settings line (switch the gloss language on the fly).** In the definition card's bottom-right (beside the dictionary citation) a dropdown lists the gloss languages available FOR THE CURRENT VIDEO LANGUAGE and switches which language the definitions are written in — nothing else about the interface changes. Mirrored by a new collapsible "Dictionary language" line in the settings panel (Auto + the source's gloss langs; gated on the Top track being definable with >1 option). Both drive ONE persisted global override (`dictionaryGlossLang`, null = auto → browser UI language). **Backend:** `Capabilities` gained `gloss_langs_by_source` ({ja:(en,de,ru…), es:(en,es)}) — Postgres derives it from one `DISTINCT (lang, gloss_lang)` scan; `/define/capabilities` exposes it (filtered to token-supported sources), `CAPABILITIES_VERSION`→2; api-client regenerated. **Client:** `glossLangsForSource(caps, src)` (lives in `define-lang.ts` to avoid a capabilities↔define-lang import cycle) + `resolveGlossLang(caps, override, sourceLang)` now restricts the override to the source's available gloss langs (a `de` override picked on JA won't leak onto an `es` video with no es→de dict). i18n: `define.glossLanguage`/`define.glossAuto` across all 12 locales. Deployed + verified live. Tests: +3 backend (42 dictionary), +6 client (356 vitest). **Rides the next store build** with the rest of the client work.

**✅ FIXED 2026-07-11 (client-only, unreleased — rides the next build) — per-word dictionary lookup DECOUPLED from the per-character annotation toggle.** Clicking a Top-line word to define it no longer requires the ruby/annotation layer to be on. Cause: the annotation toggle's only effect was the `/annotate` fetch gate (`discover.ts fetchLayerAnnotations` `if (!opts.enabled) return`), which skipped the whole batch — spans AND the per-word `tokens` — so annotation-off meant no clickable words. Fix splits fetch from render: `discover.ts` fetches when the user wants ruby (`enabled && annotate-romanize`) OR the target lang is definable (`wantTokens`); `caption-overlay.tsx` adds a render gate — with the toggle off it strips span readings (`stripReadingsIf`) so NO ruby draws while the span STRUCTURE (one segment per span) is preserved for word grouping. Applied to the main Top line + positional-extras. Alt-orthography (variant table) is a separate toggle, unaffected. Net: annotation OFF → no furigana/pinyin, but words still glow + click → card, for every definable lang. tsc + 351 vitest green.

**✅ FIXED + LIVE IN PROD 2026-07-11 — per-word vocab lookup now lights up for ALL definable languages, not just ja/zh/ko (the capability-driven pipeline is finally real client-side).** Connor verified on the dev build that ONLY Korean (plus the original ja/zh) produced clickable definitions — French/German/Spanish/Hindi returned senses from `/define` but were never clickable, so "build a new dictionary → server ingest → works, no extension release" was broken for every generic/Indic language. **Root cause:** the client groups words by SPAN INDEX (one rendered segment per annotation span), but `AnnotateToken.start/length` were CODEPOINT offsets on the generic + Indic paths — only ja/zh/ko aligned (ja is genuinely span-indexed; zh/ko spans are one-codepoint-each by coincidence). Latin generics (es/fr/de/it/pt/…) return `spans:[]` and the client collapsed the whole line to ONE plain segment → `planWordGroups(1, tokens)` couldn't subdivide. Brahmic (hi) had per-akshara spans (नमस्ते = 3 aksharas) but a 6-codepoint token → overshoot → dropped. Cyrillic (ru/uk) had per-word spans, same mismatch. **Two-part fix (`eef24f0`), no per-language client wiring — stays capability-driven:** (1) **client** (`lib/orthography/build-segments.ts`) — a no-span interactive line now emits ONE plain segment per CODEPOINT (only when word-grouping, `coalescePlain:false`; playback keeps the coalesced blob), so segment index == the codepoint offset the server uses → every Latin/whitespace generic lang becomes clickable. (2) **server** (`build_word_tokens` → `_remap_char_offsets_to_spans` in `romanize.py`) — a generic-path language that ALSO has annotation spans (Brahmic aksharas, Cyrillic words) gets its codepoint offsets remapped onto the span indices they OVERLAP (overlap, not exact-boundary, survives tokenizer skew — word-regex `привет` vs span `привет,` maps onto the whole span instead of dropping it). Latin generics keep codepoint offsets (spans empty → no remap). `ENGINE_VERSIONS` hi/ru/uk→3 (token offsets changed → invalidate cache); future Brahmic bn/ta/te/gu/pa inherit the remap for free. **Verified live in prod:** `/annotate` es `comieron`→lemma `comer` (0-span, codepoint tokens), hi `नमस्ते`→token `(0,3)` span indices (was `(0,6)`), ru `Кошки едят рыбу`→per-word `(0,1)(2,1)(4,1)`. Tests: +5 backend (hi remap / multi-word alignment / Cyrillic word map / punctuation-adjacent survival), +3 client (per-codepoint / coalesced blob / offset alignment), +stale-cache-test fix (`en` is a definable source lang now → swapped its "unannotatable" example to `vi`). **Client side rides the next store build** (0.5.0) alongside the i18n + capability-driven work below; the SERVER half is live now, so hi/ru/uk/es/fr/de are already correct end-to-end once the new client ships. Dev builds (`.output/firefox-mv2-dev` + `chrome-mv3-dev`, pointed at prod) rebuilt for Connor to re-verify.

**✅ SHIPPED 2026-07-11 (unreleased — rides the next build) — EXTENSION UI i18n.** The whole chrome (popup, onboarding, pill/dormant, settings panel, corpus-consent prompt, definition card) now renders in the user's browser UI language across **12 locales: en (canonical) + ja zh zh-Hant yue ko de fr es it uk ru**; any other browser language falls back to English. **Chinese is script-split three ways** (`resolve.ts::classifyChinese`): Simplified `zh` (CN/SG/Hans/bare), Traditional Taiwan-standard `zh-Hant` (TW/HK/MO/Hant — genuinely distinct vocab: 影片/使用者/分頁/執行, not just character conversion), and a distinct **colloquial written Cantonese** `yue` (係/嘅/唔/咗/睇/冇…, Traditional script). **`zh-HK` deliberately → `zh-Hant`** (HK UIs use formal Standard Written Chinese; browsers never report `yue`), so the `yue` table ships ready but is dormant until a **manual UI-language picker** exists (browser-language auto-detect can't reach it) — noted as the natural follow-up. This closes the barrier a real Japanese user (learning Chinese on iQIYI, found via AMO review) flagged. Architecture (`apps/extension/I18N.md`, `lib/i18n/`): **framework-free module singleton** — the UI locale is fixed at load (browser language), so `t(key, params?)` reads a module-level table with NO React context/provider; components just `import { t }`. `strings/en.ts` is the **single source of truth** (143 keys, `{param}` placeholders); each locale is a `Partial` of it (missing key → English fallback, so partial translations are safe). `resolveUiLocale()` maps BCP-47 → locale (all Chinese variants → `zh`); `languageName(code)` uses **Intl.DisplayNames** so the 49-entry language picker localizes for free (codes untouched — behavior-preserving). `initUiLocale()` runs once per entrypoint (popup/onboarding `main.tsx`; the 5 content scripts via `loom-app.tsx` module load). **Load-bearing rule: never call `t()` at module top level** — `initUiLocale()` runs after top-level consts evaluate, so const label-tables (`SUPPORTED_LANGS`/position/font/long-vowel) were converted to **render-time functions** (`supportedLangs()` etc.). Translations were produced by 9 parallel per-language Opus localizers against a shared glossary (Video/User language, Top/Bottom, annotation, romanization, Distinct/Merged, etc.) with proper nouns (platform names, Pinyin/Romaji/IPA/…, Noto, "Loom", `asr` badge) kept literal. `i18n.test.ts` locks every locale to the canonical key set AND asserts every `{placeholder}` survives translation (drift fails CI). Deliberately NOT localized: the manifest (`_locales/`/`__MSG_*` — low priority), API-sourced preset/group labels + `@loom/orthography-tables` variant hints (server/package data). Tests: **+44 vitest → 348** (16 files); tsc + Firefox build green. **Rides the next store build** (same client gap as the capability-driven dictionary work).

**v0.3.0 — what shipped this session (platform-expansion EXECUTED):**
- **iQIYI (iq.com)** — ZH-learner platform. Track list is SERVER-SIDE-RENDERED into the page's `__NEXT_DATA__` (`props…prePlayerData.dash.data.program.stl`) — read from the DOM, NOT a network hook (the documented `/dash` endpoint isn't fetched client-side; iq.com's other APIs are JSONP). WebVTT from `meta.video.iqiyi.com` (ACAO:* → Chrome-OK). Reuses `parseVtt`. Anchor = `.iqp-player` (the fullscreen element). Files: `platform/iqiyi.ts`, `entrypoints/iqiyi{-main}.content.ts(x)`, `overlay/iqiyi-player-anchor.ts`.
- **WeTV (wetv.vip)** — Tencent C-dramas (Duolingo cross-promo tailwind). Track list from the player's JSONP `play.wetv.vip/getvinfo` (`getinfo_callback_N({…})` via `<script>`) — intercepted by wrapping the global callback (OBSERVE, never forge the cKey). `sfl.fi[]` → WebVTT from `cffaws.wetvinfo.com`; some URLs are `.vtt.m3u8` HLS wrappers → `lib/captions/wetv/parse-m3u8.ts`. Anchor = `#player-wrapper`; WeTV REBUILDS the player subtree on resume, so the ISO entrypoint replaces WXT `autoMount` with STATE-BASED mount reconciliation (MutationObserver + 1s backstop). Native subs hidden via `[class*="text-track"]`.
- **CORS (backend, PUSHED to main/Railway):** annotate/romanize fetches carry the streaming site's PAGE origin → Chrome MV3 enforces it. Added iq.com + wetv.vip to `loom_api/cors.py` AND made the allow-list **env-extensible** (`LOOM_CORS_ORIGINS` now APPENDS) → future platforms = a Railway env var, no code/redeploy. Subtitle CDNs send ACAO:* so file fetches need no proxy.
- **UX polish:** "User language" (was "Your language"); last "Target/Native" de-jargoned; platform-aware "no auto-translation on X"; popup "compatible videos"; zh-Hant alt-orthography ON by default; default SPLIT layout (video → top-1, user → bottom-2); gold-dust glow on the dormant pill (first-run activation cue; GPU-only, document-activity wake).
- **Crunchyroll DROPPED** — fails the same-language-sub gate (translated-only, no JP). Parked scaffold removed (`80696fc`); recoverable from `1413cb7` (incl. tested ASS parser).
- **Known limitation:** native Picture-in-Picture shows no Loom overlay (PiP = raw video frame, no DOM; inherent). `apps/extension/KNOWN_LIMITATIONS.md`. Reviewer kit: `SUBMISSION_0.3.0.md`. Tests: **220** vitest (10 files; dropped 19 ASS tests with Crunchyroll).

**THE PLATFORM GATE (applies to every future platform):** decisive criterion is SAME-LANGUAGE SOFT subtitles — a fetchable text track in the language SPOKEN. Verify EMPIRICALLY (survey ~15 titles) before building. Per-platform also: (1) the overlay anchor MUST be the fullscreen element OR a descendant (an ancestor → overlay vanishes in fullscreen); NEVER a comma-list anchor (querySelector returns the outermost = an ancestor); (2) churny players need state-based mount reconciliation, not `autoMount`; (3) add the page origin to `loom_api/cors.py` (or the env var) + redeploy, or annotate/romanize 400 on Chrome (Firefox masks it); (4) get a HAR early when a network hook sees nothing. Memories: `loom-platform-expansion-gate`, `loom-platform-seam`.

**Platform-expansion candidate analysis (EXECUTED — iQIYI + WeTV shipped in 0.3.0, Crunchyroll dropped; kept below for reference/rationale).** Candidates floated: Crunchyroll, HBO Max (Max), WeTV, iQIYI (+ open to others). The architecture is READY for this: the `CaptionPlatform` seam (`lib/captions/platform/`) + the shared pipeline (overlay / annotate / romanize / settings / playhead / the 0.2.2 SPA `autoMount` lifecycle) carry over unchanged. A new platform ≈ one `CaptionPlatform` impl + one MAIN/content entrypoint per site, differing mainly in (a) how subtitle TEXT is acquired and (b) player / video / anchor selectors.

**Hard constraint: Loom needs FETCHABLE TEXT subtitles** (WebVTT / TTML-text / ASS), not burned-into-video or bitmap/image subs (those are OCR-only — step 6, out of scope). The decisive per-platform recon question is which kind a platform serves.

**Claude's read on candidates (recommendation; Connor decides):**
- **Crunchyroll — strongest.** Bullseye audience (anime / JP learners), text subtitles (ASS), SPA; acquisition likely webRequest-interceptable like YouTube. Do first.
- **Viki (Rakuten) — suggested addition.** K/C-drama learners, definitely text subs, SPA. Strong second.
- **Disney+** — text subs (WebVTT/TTML), DRM/manifest shape like Netflix; decent, some anime regions.
- **iQIYI / WeTV** — aligned audience (Chinese learners) BUT real risk of BURNED-IN subs on many titles → must verify before committing; coverage may be partial.
- **HBO Max (Max)** — weakest fit: mostly English-origin content, little for the foreign-language-learning core use case.

**Approach for "one-shot many": recon-first.** Per candidate, a cheap recon (open a title, devtools network: is there a fetchable text-subtitle request? text vs image? player/video/anchor selectors?) gates tractability. Clean text-sub platforms (Crunchyroll, Viki, Disney+) are plausibly fast given the reusable harness; the Chinese platforms need the burned-in check first. Sequence by audience-fit × acquisition-tractability. NOTE (memory `feedback_extension_caption_verification`): caption-acquisition logic can't be settled by unit tests — every new platform needs a live devtools/network recon + prod verification, exactly like the YT/Netflix work.

---

**Just shipped (v0.2.2) — reference:**

**v0.2.1 is LIVE** on AMO + Chrome (passed review ~2026-06-21). **v0.2.2 shipped this session** — built + committed + pushed (`6900c93` → `origin/main`); store upload is the only remaining task (Connor's). AMO release-notes + reviewer-notes copy (≤3000 chars each) was drafted this session. Re-upload kit: `SUBMISSION_0.2.2.md`; artifacts in `apps/extension/.output/` (`loomextension-0.2.2-{firefox,chrome}.zip` + `loom-source-0.2.2.zip`). Tests: **215** vitest (9 spec files; +3 `reduceWatchChange`). **always ship the full-monorepo source zip** (`git archive --format=zip -o apps/extension/.output/loom-source-<ver>.zip HEAD`), never WXT's partial `*-sources.zip` (omits `@loom/*` + lockfile → unbuildable). Reviewer build = `npm ci` at root → `cd apps/extension && npm run build:firefox:prod`.

**v0.2.2 headline — NO-REFRESH SPA ACTIVATION on BOTH platforms (all live-verified on Apothecary Diaries binge + YouTube home→video).** Both sites are SPAs: navigating home→video, video→video, Netflix autoplay, and manual "next episode" change the URL via `history.pushState` with NO document reload, so the old `/watch*`-only content scripts never injected on those navs → Loom was dead until F5. Fixes:
- **Broadened content-script `matches`** from watch-only to whole-site (`*://*.youtube.com/*`, `*://*.netflix.com/*`) on all four scripts so they load on the first page and survive SPA nav. NO new permissions/hosts — `host_permissions` already covered both sites; only the (narrower) `matches` widened. They no-op off /watch.
- **`autoMount` owns the overlay lifecycle** (both `content.tsx` + `netflix.content.tsx`): WXT watches the player anchor (`#movie_player` / `div[data-uia="player"]`) and mounts when it appears, unmounts when removed, **re-mounts when the player subtree is rebuilt** — which is exactly what Netflix MANUAL advance does (autoplay reuses the player). The old one-shot `ui.mount()` left the shadow host orphaned in the detached subtree → invisible pill+subs. Replaced the manual `waitForElement`+mount/unmount (both `waitForElement` copies deleted).
- **Netflix episode swap is URL-driven, not media-event-driven (KEY INSIGHT).** Netflix plays via **MSE** — it reuses ONE `<video>` element and feeds episodes through a SourceBuffer, so episode changes fire **NO `loadstart`/`emptied`**; the 0.2.1 media-swap anchor waited for events MSE never sends, so `active` stuck on ep1 and every new episode replayed ep1's events. Now the ISO posts `{type:"watch-changed", videoId}` on `wxt:locationchange`, and MAIN adopts the held manifest for that id via the new pure reducer `reduceWatchChange` (`manifest-tracker.ts`, +3 tests). (`reduceMediaSwap`/the loadstart watcher are kept as a harmless secondary path; the watcher's old `[data-uia="player"]` filter was also wrong — the `<video>` lives under `#appMountPoint` — so it's loosened + logs container.)
- **Home-preview / `/title` pollution gate** (`netflix-main.onManifest`): with the MAIN hook now running site-wide it sees manifests from home auto-previews + detail pages; it now ignores any manifest whose `movieId` ≠ the current `/watch/<id>` URL **until** a title is active (then the tracker's prefetch logic takes over). Without this, the home-screen preview (e.g. Avatar on a `/title/` page) became "active" and the real episode got held → wrong subs.
- **Reload-safe MAIN hook** (`netflix-main`): Firefox re-injects the MAIN script into an open tab on every extension reload, and re-wrapping `JSON.parse`/`stringify` STACKED wrappers → multiple generations with divergent tracker state (seen as two `active` titles for one manifest — masqueraded as "two extensions installed"). Now the window-level hooks install ONCE behind a `window["__loomNflxMainHolder_"+MAIN_SOURCE]` sentinel and route through swappable handlers; re-injection just swaps them in. (yt-main reads `#movie_player` on demand — no window patch — so no holder needed; its `yt-navigate-finish` handler already re-discovers across YT SPA navs, it just needed to be injected, hence the broadened matches + a `readVideoId()` gate on the initial `run()`.)

**Plus the settings/clarity round (also in 0.2.2):** de-jargoned panel labels ("Target/Native"→"Video language/Your language", "Annotation"→"Per-character annotation", "Romanization"→"Romanization (phonetic line)"; `describeProcessing` IAST/RR cleaned), a language-named phonetic-system picker (`phoneticSystemLabelFor` in `lang-support.ts`), **5h-5 Netflix platform-awareness** (hide `tlang` translate UI + manual/ASR badges via `getPlatform()`; image-only empty-state message; native null-track label), and the popup toggle copy (now "Loom appears automatically…", since the no-refresh fix made the earlier "refresh (F5)" guidance obsolete).

**Dev-testing gotcha learned this session:** after reloading the dev add-on, **hard-reload the Netflix/YouTube tab once** — the MAIN-world `JSON` wrappers live in the PAGE, so an extension reload without a page reload leaves the old generation's wrappers stacked (the reload-safe holder prevents this going forward, but a tab already polluted before this build needs one F5). UI issue log: `apps/extension/UI_REVISIONS.md`.

**Claude's recommended next thrust: Step 6 — OCR / Synthetic Visual Engine pipeline.** Rationale: the extension is now a mature, shipped data source on YouTube **and** Netflix — exactly what Step 6 consumes — and the OCR sub-project is the **demonstrable artifact for PhD applications (Dec 2026 – Jan 2027) with a Sept 2026 demo target**, so it carries the real deadline. The owed `opt_in_training` wire-up (from 5f) is the seam connecting the extension's data flow to Step 6. Detailed in the `Synthetic Visual Engine — Phase 1` doc + `memory/reference_ocr_roadmap.md` (ask Connor before assuming its contents). **This is a recommendation, not a decision — Connor picks the thrust.** Alternative: finish a UI-polish round 2 + close the extension loose-ends below first.

**Near-term extension backlog (cheap, opportunistic — handle as reviewer feedback lands / between larger work):**
1. **I-2 Hindi romanization not appearing** — capability exists end-to-end (`hi`→indic→`romanize`; backend IAST via aksharamukha; romanize fetch already carries `phonetic_system`). Needs ONE live prod test: load a Hindi title on the dev build, read the `[Loom Romanize]` devlog — it distinguishes fetch-fail / no-phonetic-layer / rendered-nothing (`UI_REVISIONS.md` I-2). Likely a lang-code-resolution or line-render gap, not missing impl.
2. **UI elicitation round 2** — Connor had "more" issues beyond batch 1 (C-1…C-8 all done); `UI_REVISIONS.md` is the running tracker. Known residue: the alt-orth settings **preview** still visually draws Simplified *below* the reading while the live overlay draws it *above* (C-7 residue, cosmetic).
3. **5h-5 Netflix settings-panel platform awareness** (still open) — hide `tlang=` UI when `!supportsTranslate`; drop the manual/ASR badge on Netflix; "image-only on this title" degradation (the `no-captions` path already emits). _(The "more native languages" sub-item is DONE — ko / zh-Hans verified live in 0.2.1.)_
4. **Owed extension follow-ups:** **tlang=en parser anomaly** (`&tlang=en` → full ~64 kB body but json3 extracts only 1 event; hypothesis: word-level `segs`); **Chrome MV3 runtime verify** (`npm run build:chrome`; load-unpacked + re-verify — `world:"MAIN"` is MV3-native); **stale-URL on rapid SPA nav** (likely fine, keyed by id).
5. **4g** (delete Streamlit) — lands anytime: remove `loom_app.py` + `app/`, drop `streamlit`/`pandas`/`pyarrow`/`pydeck`/`altair` from `requirements.txt`.

**Dev-build testing gotcha (cost us a confused reload 2026-06-19):** `build:firefox:dev` writes to `.output/firefox-mv2-dev/` (identity "Loom (Dev)"); `build:firefox`/`:prod` write to `.output/firefox-mv2/` (identity "Loom"). Connor reloads the temp add-on from the **`-dev`** folder. When a change is for him to live-test, build the **dev** variant (`LOOM_API_BASE=https://api.loom.nerv-analytic.ai npm run build:firefox:dev`) — a prod build never updates the folder he reloads from. See `memory/feedback_dev_build_output_dir.md`.

**Deploy mechanism:** Railway tracks `main` only — push via `git push origin monorepo-restructure:main` (fast-forward; `monorepo-restructure` is always a strict ancestor of `main` after merge). The backend redeploys only when `loom_api`/`loom_core` paths change (Railway `watchPatterns`); extension-only commits don't trigger one. Vercel (web app) also tracks `main`.

**Extension dev build against the live API (backend diagnosis — `47b5fcf`):** `LOOM_API_BASE=https://api.loom.nerv-analytic.ai npm run build:firefox:dev` → a dev-identity build ("Loom (Dev)", id `loom-dev@nerv-analytic.ai`, separate storage, verbose logs, dev-only owner-key field) pointed at the LIVE Railway API instead of localhost. Install side-by-side with the daily-driver via `about:debugging` → Load Temporary Add-on; lets you test extension changes (incl. Netflix) against real backend behavior. `LOOM_API_BASE` overrides the `--mode` default in `wxt.config.ts` (host_permissions follows it so the cross-origin `/annotate`+`/romanize` fetches aren't CORS-blocked); unset = identical to before, so prod CI is unaffected.

**Step 5 substeps:** **5a–5f ✅ shipped** (YouTube extension end-to-end, commits `2bc507c`→`fa760f3`): WXT foundation + popup + background; `yt-main.content.ts` MAIN-world tracklist read + `background.ts` webRequest pot-capture + lang-swap acquisition; dual-subs overlay (`caption-overlay.tsx`, `inheritStyles:true`); `/annotate/batch` per-token ruby (`lib/annotate/`, `annotated-text.tsx`); `/romanize/batch` full-utterance phonetic line (`lib/romanize/`); full settings panel (`settings-panel.tsx` — track switching, per-layer tlang, position picker, per-layer styling, presets, color wheel). Load-bearing gotchas live in **Tripwires** below. _(Still owed from 5f: `opt_in_training` flag wire-up — lands with step 6's OCR pipeline.)_

**5g ✅ public launch (now superseded by 0.2.0):** dev/prod split (`4f96296`); store kit (`525c46b`+`e82bc7c`: privacy page, icons, AMO `data_collection: websiteContent`, `STORE_LISTING.md`+`SIGNING.md`); global on/off kill switch + dev-only owner key (`lib/enabled.ts`); `/support`+`/donate` pages. 0.1.7 was the first YouTube-only public version. **Current submission is v0.2.0** (Netflix + UI overhaul, `e3b098d`) — field copy `SUBMISSION_0.2.0.md`; artifacts `apps/extension/.output/loomextension-0.2.0-{firefox,chrome}.zip` + `loom-source-0.2.0.zip`. **In flight:** AMO + Chrome review of 0.2.0.

**5i ✅ UI/defaults overhaul (shipped, `d944b45`).** Settings panel reorganized into 4 self-contained line-cards. 5i-1 pastel defaults + Simplified-'same as Top'; 5i-2 dot-in-pill `Switch` + native-annotation under Advanced; 5i-3 capability-driven phonetic-system picker (`phoneticSystemsFor`) + language-gated long-vowel/alt-orth; 5i-4 consolidation (`LayerStyleBlock` `children` slot); 5i-5 first-class per-line opacity + linked Top-group; C-8 per-line `Show line` master enable. C-6 collapse-to-one-line shipped then reverted (confusing). Full log: `apps/extension/UI_REVISIONS.md`.

**Step 5h — NETFLIX PORT (first pass ✅, live-verified 2026-06-19).** Governing rule: text-vs-image tracks **origin language** (ja/ko/zh/th/hi serve WebVTT on native-origin content). One `CaptionPlatform` impl + one MAIN entrypoint per site; the shared pipeline carries over unchanged. Full recon + Findings log in `NETFLIX_RECON.md`.
- **5h-1** `lib/captions/platform/{types,youtube,index}.ts` — `CaptionPlatform` acquisition seam (`acquireSession`+`fetchTrackEvents`+`supportsTranslate`); YouTube's pot-capture relocated behind it.
- **5h-2** (`878886a`) `netflix-main.content.ts` MAIN-world `JSON.parse`/`JSON.stringify` manifest hook (`document_start`) → same `loom-main`/`tracklist` message; filters `!forced && !none && hasWebVtt`; `platform/netflix.ts` (`acquireSession`→`{ok,handle:null}`, signed-WebVTT fetch, `supportsTranslate:false`); `netflix/parse-vtt.ts`; netflix.com+nflxvideo.net host perms. Image-only titles emit `no-captions` (for 5h-5 degradation).
- **5h-3** (`8650984`+`7d3b355`) `netflix.content.tsx` overlay anchored to `div[data-uia="player"]` (LCA of video + Netflix chrome → pill clickable above the controls; `video-canvas` trapped it one stacking-context below); overlay seam on `CaptionPlatform` (`playerRootSelector`/`videoSelector`/`hideNativeCaptions`, generalizing `player-scale.ts`/`stream.ts`/`caption-context.tsx`); `netflix-player-anchor.ts`; pill repositioned off the report-flag (`pill-position.ts`); pill/panel click isolation from Netflix play/pause (`stop-player-events.ts`). Playhead reuses `<video>.timeupdate`.
- **5h-4** (`7d3b355`) per-track stable `id` (YT vssId / NF trackId) + `isCc` flag on `CaptionTrack`; `auto-pick` ranks manual>asr then standard `subtitles`>SDH `closedcaptions`, CC-fallback when CC-only; fixes same-language-duplicate collisions (picker key/highlight + events cache now keyed by `id`).
- **5h-5** 🔲 STILL DEFERRED (the UI overhaul shipped without it) — settings-panel platform awareness (hide `tlang=` when `!supportsTranslate`; no manual/ASR badge on Netflix) + "image-only on this title" degradation. Now in the near-term backlog above. _(The "run more native languages beyond ja/zh-Hant" sub-item is DONE: ko / zh-Hans verified live in 0.2.1's audio-language testing.)_
- **5h-6 ✅ (0.2.1, `3633a64`) mid-episode track-switch fix + audio-language default.** Netflix's end-of-episode prefetch (next manifest + URL move while still playing the current episode) was being adopted → reverted the foreign track to Chinese + rendered next-episode subs against the current playhead. Fix: anchor "what's playing" to the `<video>` `loadstart`/`emptied` media-swap; hold any different-title manifest as `pending` until a real swap. Extracted to the pure, unit-tested reducer `lib/captions/netflix/manifest-tracker.ts`. Also: Top defaults to the title's audio language (`audioLangCode` → `pickTarget`). Plus dev/prod postMessage channel namespacing (`lib/env.ts`) after a side-by-side prod-build contamination masqueraded as the fix failing. See Tripwires.

**Architecture (locked 2026-05-03 — Option B, all-client + romanization API):** browser runs ffmpeg.wasm for video probe/extract/mux + JS ports of ASS generation + PGS rasterization (via html2canvas — see `ROADMAP.md` for why not SVG-foreignObject).  Server (`api.loom.nerv-analytic.ai` on Railway) only handles romanization: text-in / text-out, ~100KB request.  Drops backend bandwidth ~99% vs upload-everything; target hosting cost $5/mo flat.  Tradeoffs accepted: ~50MB initial JS bundle (one-time, cached), JS reimplementations of `loom_core/subs/processing.py::generate_ass_file` + `loom_core/rasterize/sup_writer.py` that must track the Python reference (drift risk — single source of truth lives in Python; JS port is a transcription), weak-device fallback to a future server-mode toggle.

**Step 4 (Option B web app) — ✅ complete except 4g.** 4a–4f shipped: npm workspaces + `apps/web` Next.js + `@loom/api-client` from OpenAPI; ffmpeg.wasm probe/extract/mux (`FFmpegClient`); full client-side `.ass`+`.sup` generation (`apps/web/lib/{subs,raster}/`, `LoomGenerator`); slim text API (`loom_api/web.py` + `routes/{romanize,annotate}.py`); deployed live at `https://loom.nerv-analytic.ai` + `https://api.loom.nerv-analytic.ai` (Railway + Vercel, slowapi rate-limits, Tier-A bypass auth). **4g 🔲** (lands anytime): delete Streamlit — remove `loom_app.py` + `app/`, drop `streamlit`/`pandas`/`pyarrow`/`pydeck`/`altair` from `requirements.txt`, update Project Structure below.

**Hosting + domain (live):** frontend on Vercel as `https://loom.nerv-analytic.ai` (custom domain CNAME → `dfa544d4c362bfd9.vercel-dns-017.com`); API on Railway as `https://api.loom.nerv-analytic.ai` (custom domain CNAME → `xsbnnuf3.up.railway.app`, plus `_railway-verify.api.loom` TXT for SSL).  Namecheap is the registrar.  Cost: $5/mo Railway hobby tier + $0/mo Vercel hobby = $5/mo flat (per the original target).

**Auth + rate limiting (live):** slowapi `100/minute,2000/day` per IP (override via `LOOM_RATE_LIMIT` env), 5000-char `text` field cap on `/romanize` + `/annotate` request models.  Owner bypass via `LOOM_BYPASS_KEYS` env + `X-Loom-Auth` header — see Owner Auth Roadmap section.

**Step 4 deferred follow-ups:**
- **Desktop backfill onto `@loom/api-client`** — 4a-5 attempt surfaced 9 legitimate type errors (generated types are stricter than hand-written ones — proper literal unions like `phonetic_system`, `null` vs `undefined` distinctions on optional fields).  Needs per-call-site refactor, not a 5-min rewrite.  Drift risk bounded as long as backend changes propagate to `apps/desktop/src/api.ts` + `apps/desktop/src/styles.ts` in the same commit.

---

## Project Structure

```
loom_app.py                # Streamlit entry point — kept as a dev/debug client through step 3b. Deletes when web app ships (step 4).
app/
  state.py                 # Streamlit session state (Streamlit-only, stays here)
  ui.py                    # Streamlit widgets, OCR buttons, native file picker (zenity/kdialog/tkinter fallback)
loom_api/                  # FastAPI service over loom_core. Hosted as Tauri sidecar (step 3) and production web service (step 4).
  main.py                  # FastAPI app + CORS middleware (allow_origins=["*"] for dev — tighten before prod)
  storage.py               # Storage Protocol + LocalFileStorage (in-process UUID→path map). S3FileStorage drops in at step 4.
  jobs.py                  # JobManager — in-process {id: JobStatus} dict + asyncio.Tasks. Swap for arq+Redis if web scaling demands it.
  deps.py                  # FastAPI dependency providers (get_storage, get_jobs)
  routes/
    health.py              # GET / and GET /health
    files.py               # POST /files (multipart upload) + GET /files/{id} (download)
    language.py            # GET /language/config/{code} → wire-safe LanguageMetadata
    generate.py            # POST /generate/ass (sync) + POST /generate/pgs (async → JobAccepted) + POST /generate/suggest-filename
    jobs.py                # GET /jobs/{id} → JobStatus
    video.py               # POST /video/scan → VideoMetadata + TrackInfo[]
    subs.py                # POST /subs/detect-language + POST /subs/detect-styles
    align.py               # POST /align → AlignResponse
    preview.py             # POST /preview → composite HTML + raw text fields
    styles.py              # GET /styles/fonts + GET /styles/presets?lang=
    mux.py                 # POST /mux → JobAccepted (writes ffmpeg output direct to client-supplied path)
apps/
  desktop/                 # Tauri 2 + Vite + React (TypeScript) — desktop shell. Step 3a foundation; step 3b builds out the UI.
    src-tauri/             # Rust shell. lib.rs spawns uvicorn loom_api.main:app as a child process; kills it on window close.
    src/                   # React frontend. App.tsx orchestrates file slots + scan; styles.ts holds StyleConfig wire types + defaults + preset apply; section components in src/sections/.
loom_core/                 # Pure engine — no Streamlit imports. Consumed by loom_app.py + loom_api.
  models.py                # Pydantic wire contracts: StyleConfig, TrackInfo, LanguageMetadata, Generate*Request, JobStatus, etc.
  language.py              # Language detection + Cantonese discriminator + script analysis + is_rtl_text
  romanize.py              # Romanization: Pinyin, Zhuyin, Jyutping, Japanese (MeCab/fugashi), Korean, Cyrillic, Thai (3 systems), Indic (6), Hebrew, Arabic/Persian/Urdu (shared walker)
  styles.py                # get_lang_config() factory with variant + phonetic_system support
  color_presets.py         # Color preset system: 28 presets (classic/cultural/dark/adaptive), language-scoped
  korean_rr.py             # Standalone Korean Revised Romanization implementation
  fonts.py                 # FontScanner (fontTools-only directory walker) + validate_font() + module-level default scanner; LOOM_FONT_DIR env var override
  subs/
    utils.py               # Shared subtitle loading + mtime-based SSAFile caching + shift_events() + compute_subtitle_offset()
    processing.py          # ASS generation + PGS generation + union timeline + concurrent event merge + opencc + style mapping + output filename builder
    preview.py             # Composite HTML preview
  video/
    mkv_handler.py         # Video scan/extract/screenshot/mux — all ffmpeg calls (any container in, MKV out)
    ocr.py                 # PGS OCR: SUP parser + Tesseract + parallel thread pool
  rasterize/
    pgs.py                 # Playwright async full-frame subtitle rasterizer (N-worker parallel pool, batched streaming)
    sup_writer.py          # PGS/SUP binary writer (inverse of ocr.py parser); batch + streaming APIs; epoch state management
tests/                     # 567 tests across 19 files. See Test Corpus below for sample-data assumptions.
.github/workflows/
  ci.yml                   # CI matrix: Ubuntu + macOS green; Windows scaffolded (System deps step pre-written, intentionally no fontconfig). pytest + Playwright Chromium + font-validator self-check on every push to main/monorepo-restructure + PRs to main.
requirements.txt
CLAUDE.md
```

---

## Monorepo Restructure Roadmap

| Step | Status | Scope |
|------|--------|-------|
| 1 → 3b | ✅ | `loom_core` carved out, FastAPI service complete (sync + async + jobs + storage), Tauri shell + sidecar IPC, full UI parity with Streamlit (file pickers, video scan + track selector, dual-view style editor, preview, generate ASS/PGS, mux, timing offsets + auto-align, filename builder + audio-default selector). Streamlit kept as dev/debug client until step 4. |
| CI ph 1–3 | ✅ | GitHub Actions matrix on push to main/monorepo-restructure + PRs to main. Ubuntu + macOS + Windows all green. Includes pytest + Playwright Chromium rasterize + font-validator self-check. fontconfig is no longer installed on any platform — `loom_core/fonts.py` is fontTools-only, same code path everywhere. |
| 3c | 🔲 | Bundling for distribution. PyInstaller / `uv` / PyOxidizer decision deferred — research prompt prepared for web Claude. Ships installers via GitHub Releases + Tauri auto-updater. |
| 4 | 🔲 | Next.js web on Vercel. Same Next.js build → either CNAMEd `loom.nerv-analytic.ai` or `apps/web/` workspace. Swap `LocalFileStorage` for `S3FileStorage`. Constrain to subtitle-only + YouTube URL flows (no large video uploads). Extract shared React components into `packages/ui/` once a second consumer exists. |
| 5 | 🟢 5a–5i ✅; 5h (Netflix) ✅; **v0.2.1 submitted to AMO + Chrome 2026-06-22** | WXT browser extension at `apps/extension/`. 5a–5f shipped May 2026 (`2bc507c`→`fa760f3`). **Netflix port (5h-1→5h-4) ✅ live-verified 2026-06-19** — MAIN-world JSON.parse manifest hook + `div[data-uia="player"]` overlay anchor. **UI/defaults overhaul (5i) ✅** (`d944b45`). v0.2.0 (Netflix + overhaul) submitted 2026-06-19 (`e3b098d`). **5h-6 ✅ (v0.2.1, `3633a64`)** — mid-episode track-switch fix (media-swap-anchored `manifest-tracker`) + audio-language target default + romanize/Pinyin defaults + outline/pill polish + collapsible settings + dev/prod channel namespacing; reviewer kit `SUBMISSION_0.2.1.md`. YouTube caption access uses webRequest interception + lang-swap (PO-token moat in `memory/reference_youtube_caption_acquisition_2026.md`). **Next: Connor picks the thrust — Claude recommends Step 6 OCR (the PhD-apps artifact).** Major OCR data source from 5f's `opt_in_training` toggle wire-up (still owed). |
| 6 | 🔲 (parallel) | OCR pipeline as separate `loom_ocr/` package. Closed-loop synthetic data → fine-tuned TrOCR. Runs as a batch process, not part of the API. Detailed in `Synthetic Visual Engine — Phase 1` doc; targets Sept 2026 demo for PhD applications. |

**Locked tech decisions for steps 3+:**
- Frontend: Vite + React (not Next.js) for desktop. Web app at step 4 may migrate to Next.js, with shared components in `packages/ui/`. Don't extract the package until a second consumer exists — premature shared libraries are how API ergonomics go bad.
- IPC: HTTP on localhost (not Tauri commands). Frontend stays deployment-agnostic — same code talks to localhost sidecar or `https://api.loom.nerv-analytic.ai`. One env var flips the base URL.
- Storage: `Storage` Protocol now, `LocalFileStorage` only impl until step 4. `S3FileStorage` drops in without route changes.
- Job runner: in-process dict + `asyncio.Task`. Migrate to arq+Redis only if/when web traffic outgrows one uvicorn worker. Tauri sidecar will never need persistence (process dies with the app).
- Python bundling: defer until step 3c. Dev mode uses the developer's existing Python (env vars `LOOM_UVICORN`, `LOOM_PROJECT_ROOT`, `LOOM_SIDECAR_PORT` override defaults).
- OCR data ingestion: `opt_in_training: bool = False` baked into request models from step 2c. No archival code yet — wires up at step 5 when the extension produces real data flow. Privacy-hedge in place from day one.

---

## Owner Auth Roadmap

Production rate limits (slowapi 100/min, 2000/day per IP; 5000-char per-request cap) protect the slim API from abuse but also block legitimate high-volume operator use — notably the Step 6 OCR synthetic-data pipeline, which will fan out tens of thousands of romanize/annotate calls. The owner-auth path lets Connor (and only Connor) bypass the limiter without weakening defenses for everyone else. Three additive tiers; A satisfies v1.

**Tier A — pre-shared bypass key (✅ shipped, live).** Secret(s) live in Railway env `LOOM_BYPASS_KEYS` (comma-separated, supports rotation). Visit `loom.nerv-analytic.ai/?owner_key=<secret>` once per device → `OwnerKeyBootstrap` (`apps/web/components/owner-key-bootstrap.tsx`) stashes it in `localStorage.loom_owner_key` and cleans the URL. Every API call then carries `X-Loom-Auth: <secret>` via the `openapi-fetch` middleware in `apps/web/lib/api/client.ts` (read per-request, so a fresh value takes effect immediately). Server-side, `BypassAwareSlowAPI` (`loom_api/web.py`) wraps `SlowAPIMiddleware` and skips the limiter *entirely* for allow-listed keys (`hmac.compare_digest`, constant-time). A floating "owner mode" pill shows when active. **Reset:** `localStorage.removeItem("loom_owner_key")` or visit `/?owner_key=`. **Rotate:** change `LOOM_BYPASS_KEYS` (invalidates all devices at once) and re-issue. Limitations (acceptable for v1): per-device not per-identity; key briefly appears in URL/history; no per-device revocation.

**Tiers B & C (planned / deferred).** B = Google OAuth identity binding (per-email allow-list + minted session JWT); C = Cloudflare Access network gate. Both are strictly additive over Tier A's `X-Loom-Auth` path. Full design, triggers, and cost in **`ROADMAP.md` → Owner Auth (Tiers B/C)**.

---

## Capability Matrix

**Purpose:** at-a-glance visibility into which features have reached which surfaces. Backend (`loom_core` + `loom_api`) is the single source of truth — frontends call the API, never reimplement engine logic. Frontend rows track UI affordance, not capability (a feature with backend ✅ is callable from any frontend the moment its UI lands).

**Update protocol:** when shipping a feature, add a row OR update an existing row's columns in the same commit as the code. Don't ship a backend change without updating the matrix — drift here is the failure mode this exists to prevent.

**Legend:** ✅ shipped · 🟡 partial · ⏳ planned · — N/A by design

| Feature | Engine | API | Desktop | Web | Extension |
|---|---|---|---|---|---|
| **Subtitle ingestion** | | | | | |
| `.srt` / `.ass` / `.ssa` / `.vtt` upload | ✅ | ✅ | ✅ | ✅ | ⏳ |
| Local file picker (zenity / native) | ✅ | ✅ | ✅ | — | — |
| External video file scan (MKV tracks) | ✅ | ✅ | ✅ | ✅ | — |
| Multi-style fansub classifier (signs / OP / ED / staff filtered out) | ✅ | ✅ | ✅ | ✅ | ⏳ |
| YouTube URL → subtitle pull (yt-dlp) | ⏳ | ⏳ | — | ⏳ | — |
| YouTube caption interception (webRequest + lang-swap; PO-token gated) | — | — | — | — | ✅ 5b |
| Streaming platforms supported (fetchable text subs) | — | — | — | — | ✅ YouTube · Netflix · iQIYI · WeTV · **Prime Video** (6) |
| Prime Video acquisition (MAIN-world `GetVodPlaybackResources` hook → TTML2) | — | — | — | — | ✅ `parse-ttml` (2026-07) |
| Positional / vertical / simultaneous cue rendering (source-position preserved) | — | — | — | — | ✅ `CueLayout`→`PositionalCue`/`zoneAnchor` (Prime + Netflix, 2026-07) |
| Real-time caption playhead tracking on streaming video | — | — | — | — | ✅ 5b |
| Dual-subs overlay above streaming-video caption area | — | — | — | — | ✅ 5c |
| Single-line mode (single-track / all-native media, dictionary-enabled) | — | — | — | — | ✅ `pickPrimary` (2026-07) |
| Live track switching mid-playback (with cached events) | — | — | — | — | ✅ 5f-diag |
| Auto-pick foreign (Top) track by the title's audio language | — | — | — | — | ✅ Netflix `audioLangCode`→`pickTarget` (5h-6/0.2.1) |
| Hold next-episode prefetch until real media swap (no mid-episode track flip) | — | — | — | — | ✅ Netflix `manifest-tracker` (5h-6/0.2.1) |
| Per-tab activation gate (dormant by default) | — | — | — | — | ✅ 5d-perf |
| Per-character annotation ruby (furigana / Pinyin / Zhuyin / Jyutping / RR) | — | — | — | — | ✅ 5d |
| `/annotate/batch` single-shot fetch | — | ✅ | — | — | ✅ 5d |
| `/romanize/batch` single-shot fetch (secondary phonetic line) | — | ✅ | — | — | ✅ 5e |
| Alternate-orthography ruby (zh-Hant ↔ Simplified) | ✅ table | — static client-side lookup | — | ⏳ | ✅ 5f |
| Per-word vocab lookup (pause → glow → click → definition card) | — | ✅ `/define/batch` | — | — | ✅ JA + ZH (0.4.0); KO backend 2026-07-10 (rides next build) |
| Dictionary-language (gloss) picker — per-source, in-card + settings | ✅ `gloss_langs_by_source` | ✅ `/define/capabilities` v2 | — | — | ✅ card dropdown + settings line (rides next build) |
| Grammar-aware breakdown of inflected words (dict form + inflection chain) | ✅ `loom_core/grammar.py` (JA + KO morphology; ~15 langs via Wiktionary `form-of` tags) | ✅ `/define/batch` `grammar` + form-of lemma resolution | — | — | ✅ card "Grammar" section, JA+KO (rides next build); hi/es/fr/de/ru/… free via form-of |
| Speaker-markup exclusion (（名） labels + multi-speaker dashes; display-preserving) | ✅ `_drop_speaker_markup_tokens(_cp)` (CJK + Latin generics) | ✅ annotate/romanize | — | — | ✅ live-effective (renders server tokens) |
| Dictionary data (JMdict ja + CC-CEDICT zh, 502k rows, CC-BY-SA) | ✅ `dictionary_entry` | ✅ store + decomposition | — | — | ✅ card + `/privacy` attribution |
| Dictionary data (KRDict ko→11 langs, LMF XML, CC-BY-SA 2.0 KR) | ✅ `parse_krdict` + `gloss_lang` axis | ✅ store (603k rows LIVE in prod) | — | — | ✅ card + `/privacy` attribution (rides next build) |
| Server-declared definable langs + gloss-language axis (`/define/capabilities`) | ✅ `is_token_supported` | ✅ endpoint + `gloss_lang` | — | — | ✅ capability-driven client (rides next build) |
| Korean word tokenizer (kiwipiepy morphology → 다-form lemmas) | ✅ `_korean_tokens` | ✅ batch routes | — | — | ✅ rides next build |
| Generic word tokenizer (simplemma, ~50 space-delimited langs; custom-first) | ✅ `_generic_tokens` | ✅ batch routes | — | — | ✅ rides next build |
| Dictionary data (Wiktextract X→English, CC-BY-SA+GFDL) | ✅ `parse_wiktextract` | ✅ store (Spanish 804k rows LIVE) | — | — | ✅ card + `/privacy` attribution (rides next build) |
| Corpus quality-check harness (gate before enabling a language) | ✅ `scripts/dict_quality_check.py` | — | — | — | — |
| Word-level `tokens` on `/annotate` (merge / contextual reading / punct-strip) | ✅ `build_word_tokens` | ✅ batch routes | — | — | ✅ JA + ZH (0.4.0) |
| Hepburn romaji in JA card (macron + doubled) | ✅ `hepburn_from_kana` | ✅ `/define` | — | — | ✅ 0.4.0 |
| Distinct / Merged tier highlight (forward-collapse marker) | ✅ data | — | — | ⏳ | ✅ 5f |
| Inline live preview of orthography pair (語→语 + 髮→发+發) | — | — | — | — | ✅ 5f |
| **Romanization** (engine + API ✅ for all) | | | | | |
| Chinese (Pinyin / Zhuyin / Jyutping) | ✅ | ✅ | ✅ | ✅ | ✅ ruby + line (5e) |
| Japanese (MeCab + furigana, 3 long-vowel modes) | ✅ | ✅ | ✅ | ✅ | ✅ ruby + line (5e) |
| Korean (RR per-syllable + word-level) | ✅ | ✅ | ✅ | ✅ | ✅ ruby + line (5e) |
| Cyrillic (ru / uk / be / sr / bg / mk / mn) | ✅ | ✅ | ✅ | ✅ | ✅ line (5e) |
| Thai (paiboon / RTGS / IPA) | ✅ | ✅ | ✅ | ✅ | ✅ line (5e) |
| Indic (hi / bn / ta / te / gu / pa) | ✅ | ✅ | ✅ | ✅ | ✅ line (5e) |
| Hebrew | ✅ | ✅ | ✅ | ✅ | ✅ line (5e) |
| Arabic / Persian / Urdu | ✅ | ✅ | ✅ | ✅ | ✅ line (5e) |
| **Output generation** | | | | | |
| `.ass` 3- or 4-layer file | ✅ | ✅ | ✅ | ✅ | ⏳ |
| `.sup` (PGS) bitmap rasterization | ✅ | ✅ | ✅ | ✅ | — |
| Live HTML composite preview | ✅ | ✅ | ✅ | ⏳ | ⏳ |
| Output filename builder | ✅ | ✅ | ✅ | ⏳ | — |
| MKV mux (ffmpeg subtitle merge) | ✅ | ✅ | ✅ | ✅ | — |
| **Style customization** | | | | | |
| Per-layer color | ✅ | ✅ | ✅ | ⏳ | ✅ 5f |
| Per-layer font family + size | ✅ | ✅ | ✅ | ⏳ | ✅ Bottom/Top/Annotation/Romanization (5e) |
| Per-layer alpha (text color opacity) | ✅ | ✅ | ✅ | ⏳ | ✅ first-class opacity slider + linked Top-group (5i-5) |
| Per-line master show/hide enable | — | — | — | — | ✅ Top/Bottom (5i C-8); Top off hides its annotation+romanization+alt-orth |
| Per-language phonetic-system picker (capability-driven) | ✅ | ✅ | ✅ | — | ✅ Chinese/Thai/Arabic/Persian/Urdu via `phoneticSystemsFor` (5i-3); zh-Hant defaults Pinyin (0.2.1) |
| Language-aware defaults (romanize line on JP-only among CJK+K; on for Cyrillic/Thai/Indic/Hebrew/Arabic) | — | — | — | — | ✅ `defaultRomanizeLineEnabledFor`/`defaultPhoneticSystemFor` (0.2.1) |
| Collapsible settings-panel sections | — | — | — | — | ✅ **default-collapsed + persisted global (storage.local) + per-section collapsed summary line** (2026-07-11); state lifted to `SettingsPanel`, survives close/reopen (0.2.1) |
| Auto-fading status pill (3s idle) | — | — | — | — | ✅ opacity-only, edge-triggered (0.2.1) |
| Per-layer outline color + alpha | ✅ | ✅ | ✅ | ⏳ | ✅ 5f; 8-dir ring + opacity-coupled, diverges from web PGS (0.2.1) |
| Per-layer glow (radius + color + alpha) | ✅ | ✅ | ✅ | ⏳ | ✅ 5f |
| Per-layer shadow | ✅ | ✅ | ✅ | ⏳ | 🟡 hardcoded black @ 0.7; not yet user-controllable |
| Inline HSV color wheel | — | — | — | ⏳ | ✅ 5f (react-colorful) |
| Top stack position + layer gaps | ✅ | ✅ | ✅ | ⏳ | 🟡 4-slot picker (top-1/2 + bottom-1/2) |
| Top/bottom vertical nudge + line spacing (per platform) | ✅ | ✅ | ✅ | ⏳ | ✅ main lines only, `zoneStyle` (2026-07) |
| Global subtitle-size multiplier (per platform) | — | — | — | — | ✅ Settings → "Subtitle size" (2026-07) |
| Picture-relative font scaling (sizes to the video picture, not player root) | — | — | — | — | ✅ `usePlayerScale` (2026-07) |
| Color presets (28, 4 categories, lang-scoped) | ✅ | ✅ | ✅ | ⏳ | ✅ 5f |
| Per-layer tlang= machine translation | — | ✅ | — | — | ✅ 5f-diag |
| Output resolution scaling (480p–2160p + match) | ✅ | ✅ | ✅ | ⏳ | — |
| **Timing / sync** | | | | | |
| Manual offset (per-track ms shift) | ✅ | ✅ | ✅ | ⏳ | ⏳ |
| Auto-alignment (histogram + fine pass) | ✅ | ✅ | ✅ | ⏳ | ⏳ |
| **Fonts** | | | | | |
| Bundled Noto manifest (29 faces) | ✅ | ✅ | ✅ | 🟡 | ⏳ |
| `@font-face` CSS w/ unicode-range routing | ✅ | ✅ | ✅ | 🟡 | ⏳ |
| FontScanner (validate + missing-char warn) | ✅ | ⏳ | ⏳ | ⏳ | ⏳ |
| **Deployment** | | | | | |
| Public web URL (`loom.nerv-analytic.ai`) | — | — | — | ✅ | — |
| Slim text-processing API (`api.loom.nerv-analytic.ai`) | — | ✅ | — | — | — |
| Rate limiting (slowapi 100/min, 2000/day per IP) | — | ✅ | — | — | — |
| Owner bypass auth (Tier A: `X-Loom-Auth`) | — | ✅ | — | ✅ | — |
| Content-addressed romanize/annotate result cache (Postgres, fail-open) | ✅ `engine_version` | ✅ batch routes | — | — | — |
| Worker recycling (gunicorn `--max-requests`, RAM-creep relief) | — | ✅ | — | — | — |
| **Distribution / packaging** | | | | | |
| Linux desktop bundle (`.deb` + `.rpm`) | — | — | ✅ | — | — |
| AppImage | — | — | ⏳ | — | — |
| macOS desktop bundle (`.app` + `.dmg`) | — | — | ⏳ | — | — |
| Windows desktop bundle (`.msi` + `.nsis`) | — | — | ⏳ | — | — |
| Tauri auto-updater (multi-hundred-MB diffs) | — | — | ⏳ | — | — |
| **Loom Player** (7c, MOBILE_ROADMAP.md) | | | | | |
| **Single-window integrated player** (libmpv render API → GtkGLArea, transparent webview composited on top) | — | — | ✅ 7c-7/8/9 (X11; live in-app) | — | — |
| Local media playback (libmpv render engine, `mpv_render.rs`) | — | — | ✅ 7c | — | — |
| **DOM caption stack** (HTML `<ruby>` relative positioning — .ass is NOT the core format, Connor 2026-07-12) | — | — | ✅ overlay payload | — | ✅ (same payload) |
| Pause → word click on the video → definition card (prod `/define`) | — | ✅ | 🟡 built; in-app click-through owed | — | — |
| Generated 4-layer .ass (test scaffold + export only; never Player core) | ✅ | ✅ `/generate/ass` | ✅ generator | — | — |
| **OCR data pipeline** (step 5 → step 6) | | | | | |
| `opt_in_training` flag on requests | ✅ | ✅ gates `/corpus/capture` + sidecar forward | ✅ checkbox default-ON | ✅ checkbox default-ON | ✅ two-touch consent (0.3.1 release pending) |
| Media-identity corpus capture (`/corpus/capture`, dedup, fail-soft) | — | ✅ | ✅ via sidecar→prod forward | ✅ `lib/api/corpus.ts` | ✅ `lib/corpus/` (0.3.1 release pending) |
| Corpus → Parquet export (R2/S3, manifest, prune) | — | ✅ `corpus_export.py` + monthly GHA | — | — | — |
| `(text, style, language)` tuple archive (styles: file sources only) | — | ✅ corpus lines ⋈ cache at export + `style`/`styles_json` | ✅ | ✅ | 🟡 text-only (no style visibility) |
| Synthetic OCR training pipeline | ⏳ | — | — | — | — |

---

## Layer Terminology — CRITICAL

**Get this right. Every time. No exceptions.**

| Layer name | Screen position | Content | Variable names |
|------------|----------------|---------|----------------|
| **Bottom** | Lowest on screen | User's **native** language (the language the user speaks, e.g. English for an English speaker) | `native_file`, `native_subs`, `native_text`, `bottom_text`, `native_lang` |
| **Top** | Above Bottom | **Foreign / media** language (the language of the video, e.g. Japanese, Thai, Korean) — this is the "target" of the processing/romanization pipeline | `target_file`, `target_subs`, `target_text`, `top_html`, `target_lang_code` |
| **Romanized** | Above Top | Phonetic transcription of the Top/foreign text (Pinyin, Romaji, etc.) | `romaji_text` |
| **Annotation** | Above individual Top tokens | Per-token readings of the Top/foreign text (furigana, bopomofo, etc.) | via `\pos()` in ASS, ruby in PGS |

- "**Native**" = user's own language. NOT the language native to the media.
- "**Target**" = the foreign language being processed/romanized. It is the "target" of the pipeline, not the user's learning target.
- `content_key = (bottom, top, romaji, preserved)` — bottom is native, top is foreign.
- `_derive_region_keys` → region 0 = top (foreign), region 1 = bottom (native).
- In `build_output_filename()`: `native_lang` = user's language code, `target_lang` = media language code.

---

## Key Architectural Decisions

**Four-layer output, two independent pipelines.** `.ass` file has 3 or 4 text layers (Bottom / Top / Romanized / optionally Annotation with `\pos()`), controlled by `include_annotations` param. PGS `.sup` is a separate full-frame bitmap rasterization. PlayResX=1920, PlayResY=1080 set explicitly on all generated `.ass` files. All coordinates and font sizes in 1080-scale.

**`.ass` pipeline** (`generate_ass_file()`): no Playwright dependency. `supports_ass_annotation`: CJK=True, R4/Indic/Hebrew=False — gates `\pos()` annotation generation (non-CJK annotation is PGS-only because the layout math assumes CJK glyph widths).

**PGS pipeline** (`generate_pgs_file()` → `rasterize_pgs_to_file()`):
- Playwright async API, N-worker parallel pool (`num_workers`, default 1). Reorder heap preserves timestamp order; consumer writes sequentially via `SupWriter`. Memory-bounded streaming write.
- Nested event loop support (Streamlit) via background thread.
- ~50–100ms per screenshot; 300 events ≈ 15–30s.
- Requires `playwright install chromium`.
- **Union timeline** (`_build_pgs_timeline()`): union of all timing boundaries from native + target tracks. One interval per segment so when only one track changes, epoch system emits a Normal update (only changed region re-encoded). Fixes flicker when tracks have independently-timed line breaks.
- **Concurrent event merging** (`_merge_concurrent_target_events()`): groups target events by identical `(start, end)`. Drops music-only events (♪, ♫) when real dialogue is concurrent; stacks remaining concurrent events with `<br>` / `\N`.
- **Canvas-aware region splitting** (`split_regions(canvas_height=)`): gap midpoint must be in 25%–75% of canvas to allow 2-region split. Prevents subtitle dropout when only top-half content is rendered.
- **Epoch management** (`SupWriter.write(region_content_keys=...)`): Epoch Start (full redraw) / Acquisition Point (every 12 display sets, for seek safety) / Normal (only changed region re-encoded) / Skip (identical content). Reserved palette ranges: obj 0 → indices 1–127, obj 1 → 128–254. Fixed windows: top 45%, bottom 25%. Abutting threshold ≤ 50ms. Clears always Epoch Start. `region_content_keys=None` falls back to Epoch Start.

**Annotation infrastructure is language-agnostic.** `get_annotation_func(lang_code)` → span producer. `build_annotation_html(spans, mode)` with 3 render modes: `"ruby"`, `"interlinear"`, `"inline"`. `annotation_font_ratio`: CJK=0.5, alphabetic=0.4. Adding a new annotated script = new `get_annotation_func()` only.

**Container-agnostic input, MKV output.** ffprobe/ffmpeg accept any container. Output always `.mkv`. Subtitle upload accepts `.srt`, `.ass`, `.ssa`, `.vtt`. `loom_core/video/mkv_handler.py` is the only file that touches ffmpeg.

**MKV mux critical flags:** `-c:s:N ass` re-encodes ASS track (fixes timestamp conversion). `-max_interleave_delta 0` forces strict DTS-order interleaving (fixes subtitle clustering). PTS=0 anchor in `SupWriter`/`write_sup()` prevents ffmpeg timestamp rebasing. `merge_subs_to_mkv()` accepts optional `ass_path` + `sup_path`; `disposition:default` on PGS if both present; `default_audio_index` sets audio default; `keep_existing_subs`/`keep_attachments` for track stripping.

**Output filenames:** `build_output_filename()` → `{media}.{year}.{native_lang}.{target_lang}[.{annotation}][.{romanization}].{ext}`. Title/year from `get_video_metadata()`.

**No RAM-loading of video** — always local path + ffmpeg subprocess.

**Timing offsets** (`shift_events(subs, offset_ms)` in `loom_core/subs/utils.py`): deep-copies SSAFile, shifts all event start/end by `offset_ms`, clamps to >=0. Applied as `native_offset_ms`/`target_offset_ms` immediately after subtitle load in preview/processing call sites. Streamlit UI uses pending-key indirection (`_pending_top_offset_sec`/`_pending_bottom_offset_sec`) to avoid `StreamlitAPIException` on post-widget state mutation.

**Auto-alignment** (`compute_subtitle_offset(reference_subs, target_subs)`): returns `target_time - reference_time` (positive = reference earlier, shift source-A tracks later). Coarse pass = pairwise-difference histogram (N×M pairs, 100ms bins, `Counter`); fine pass = ±2s around peak in 10ms steps, ±500ms tolerance, midpoint of best plateau. Filters Comment events + `\p` drawings; minimum 5 dialogue events per track.

**Output resolution scaling:** `_PLAYRES_OPTIONS` (480p–2160p) + "Match source". `_scale = target_height / 1080` applied to all style attrs.

---

## Tripwires — load-bearing, don't relearn these

**Backend (`loom_api` on Railway):**
- **Worker recycling MUST go through gunicorn, not `uvicorn --limit-max-requests`.** railway.json's `restartPolicyType` is `ON_FAILURE` — a process that exits CLEANLY (code 0, which is exactly what uvicorn does when it hits its request limit) is **never restarted** → silent total outage. The gunicorn master never exits; it respawns workers as `--max-requests` retires them. (Procfile + railway.json `startCommand`, worker class `uvicorn_worker.UvicornWorker` — the `uvicorn-worker` package, NOT the deprecated `uvicorn.workers` path.)
- **The result cache must stay fail-open.** `PostgresResultCache` swallows every DB error internally (log + 30s backoff); routes never see cache exceptions and `get_result_cache()` degrades to `NullResultCache` on any construction failure. Never let a cache/DB problem 500 the batch routes — uncached-but-working is the contract (`loom_api/result_cache.py`).
- **Changing romanizer OUTPUT requires an `ENGINE_VERSIONS` bump** (`loom_core/romanize.py`) or the cache serves the old behavior forever and "why didn't my fix take?" ensues. Refactors that don't change output must NOT bump (needless cold start).

**Extension (`apps/extension`):**
- **NO `backdrop-filter` anywhere.** The pill + panel render over YouTube's continuously-repainting player; `backdrop-filter` re-blurs the underlying pixels every frame → main-thread saturation → multi-second input lag. Use a solid `rgba(...)` background with opacity ≥ 0.94. (`components/loom-pill.tsx`, `components/settings-panel.tsx` headers.)
- **The pill MUST NOT depend on `target`/`native` from context.** A compact-mode toggle keyed on caption text re-rendered the pill every dialogue boundary, generating new inline styles that triggered overlapping CSS transitions that never settled. The pill reads only `status` and is wrapped in `React.memo`; compact mode was dropped. **(0.2.1) The active pill now auto-fades after 3s idle** — but perf-safely: it's a single `visible` boolean that flips ONLY on the idle↔active edge (never per dialogue), the `mousemove` handler short-circuits via a `visibleRef` so it never `setState`s per-move, and the fade is an `opacity` transition on the already-promoted compositor layer (GPU, no main-thread paint). Don't reintroduce per-frame state on the pill. (`components/loom-pill.tsx`.)
- **The shadow host MUST be on its own compositor layer.** `transform: translateZ(0); will-change: transform; contain: layout paint style` on `loom-overlay-root` (`injectHostPositioningStyle` in `content.tsx`), else YT's progress-bar tick + control auto-hide cascade through our paint surface on the main thread. Same `translateZ(0)` on the pill button for defense-in-depth.
- **WXT `build` defaults to `chrome-mv3`.** `npm run build` → `.output/chrome-mv3/`; Firefox testing needs `npm run build:firefox` → `.output/firefox-mv2/`. If UI changes "aren't appearing" after a reload, check `.output/firefox-mv2/` mtime before hunting a logic bug.
- **Nested-`<ruby>` outer-rt position is inverted on Firefox MV2.** Per-rt `ruby-position` is honoured for flat single-rt rubies but ignored (forced to `over`) for the outer rt of a nested ruby. **In the live overlay the Simplified auxiliary ruby therefore renders ABOVE the reading/annotation line** — the desired, kept behavior (reads better pedagogically). Any prose calling it "Simplified below" is wrong; the language coding is unchanged, only the description. NOTE the settings-panel preview still uses flat single-rt rubies and so VISUALLY shows Simplified below the reading — a known preview-vs-live divergence to reconcile in the UI overhaul (`UI_REVISIONS.md` C-7). (`annotated-text.tsx` header.)
- **react-colorful's runtime CSS auto-inject doesn't reach the shadow root.** It `appendChild`s a `<style>` to `document.head`, which our shadow DOM doesn't inherit. The CSS is vendored verbatim in `settings-panel.tsx::REACT_COLORFUL_CSS` (pinned to 5.6.1; re-extract via the regex-walker noted there when bumping).
- **`tlang=` is intrinsically lock-step.** A `tlang` override makes YouTube emit one MT'd event per source event with identical timing — that's the API, not a regression. Visible only where source-event boundaries don't match English sentence boundaries (Chinese mid-clause splits being canonical). If "tracks are no longer independent" is reported, first check whether the native side is `(auto)`/tlang.
- **(0.2.1) The overlay outline INTENTIONALLY diverges from the web PGS reference.** `caption-overlay.tsx::buildTextShadow` is an **8-direction** text-shadow ring (was corners-only, which looked chunky/"chrome"), and outline + drop-shadow + glow alpha are **multiplied by the layer's master opacity** so lowering a line's opacity fades the whole stack together (the fill used to fade alone, leaving the outline floating). This deliberately does NOT match `apps/web/lib/raster/build-html.ts::textShadowCss` — the web path rasterizes to PGS and needs pixel parity with the desktop; this live-HTML overlay does not. Don't "resync" them. (Comment in `caption-overlay.tsx`.)
- **(0.2.1) Dev/prod postMessage channel is namespaced — and MUST stay that way.** `MAIN_SOURCE`/`ISO_SOURCE` (`lib/env.ts`) get a `-dev` suffix when `IS_DEV`. A dev build is routinely loaded side-by-side with the prod "Loom" for live testing; both inject content scripts into the SAME page, which shares one `window`, so a `window.postMessage` from either build's MAIN is seen by BOTH builds' ISO listeners. With a shared tag, the installed prod build's (old-code) MAIN leaked tracklists into the dev ISO and silently overrode the dev build's behavior — this masqueraded for two cycles as "the Netflix fix isn't working." If a dev-build behavior change "has no effect," FIRST confirm the prod build isn't also installed/enabled.

**Extension — Netflix (`apps/extension`, step 5h):**
- **Anchor the overlay to `div[data-uia="player"]`, NOT `div[data-uia="video-canvas"]`.** video-canvas is a child of player; Netflix's control chrome (back/flag/bottom bar) lives in `player`, ABOVE video-canvas's stacking context — so an overlay mounted in video-canvas (even at max z-index) is trapped *below* the controls and the pill is unclickable while the chrome is up. `player` is the lowest common ancestor of the `<video>` and the controls (confirmed via LCA probe), so our max-z wins there. It also wraps video-canvas as the same box (captions/scale unchanged) and is inside the fullscreen subtree. (`lib/overlay/netflix-player-anchor.ts`.)
- **Pill/panel clicks must `stopPropagation`.** Netflix toggles play/pause on a bubble-phase `click` on the player surface (and dblclick→fullscreen). Since the overlay lives inside `player`, every pill toggle / in-menu click leaked to it. Swallowed at the pill + panel boundary via `lib/overlay/stop-player-events.ts` (stopPropagation, not preventDefault). Safe because the panel's click-outside dismiss is a CAPTURE-phase document listener (fires first). YouTube doesn't pause on overlay clicks → harmless there.
- **Identify tracks by `id`, never `languageCode`.** Netflix exposes multiple tracks per language (plain "English" + "English (CC)"); matching on languageCode collapsed them → dual-highlight, duplicate React keys, events-cache collisions. `CaptionTrack.id` (YT vssId / NF manifest trackId) is the identity for picker key + selection + cache. `isCc` (`rawTrackType==="closedcaptions"`) lets `auto-pick` prefer standard subtitles while keeping CC selectable.
- **Manifest hook runs MAIN-world at `document_start`.** The hook must monkey-patch `JSON.parse`/`JSON.stringify` BEFORE the player fetches its (MSL-encrypted) manifest. The stringify hook injects the `webvtt-lssdh-ios8` profile so the server returns WebVTT URLs. (`entrypoints/netflix-main.content.ts`.)
- **(2026-07-18, SUPERSEDES the 0.2.1 single-`pending`-slot design) Netflix SPA state = a bounded HELD-MAP keyed by movieId; adopt only on an explicit title-naming signal; NEVER drop a manifest.** Netflix is a SPA: it parses a title's MSL manifest at various times — sometimes BEFORE `history.pushState` flips the URL to that title's `/watch/<id>` (a browse→episode click), sometimes after; and it prefetches the NEXT episode's manifest to buffer ahead. The OLD design DROPPED any manifest matching neither the active title nor the URL, which killed the pre-URL-flip parse (Netflix never re-parses → **"no subs until F5"**), and never cleared `active` on leaving `/watch/` (→ **"sticky subs":** previous title's captions on the next media). The reducer (`lib/captions/netflix/manifest-tracker.ts`, PURE, 40 unit tests) now HOLDS every parsed manifest in an insertion-ordered map (bounded `HELD_MAX=8`; upsert refreshes signed URLs, never downgrades ok→no-captions) and ADOPTS (posts to ISO) only when a signal NAMES a title: `reduceManifest(…, urlMovieId)` adopts when nothing active AND `movieId===urlMovieId` (fresh page load); `reduceWatchChange(videoId)` adopts the held manifest when the URL moves to `/watch/<videoId>` (the reliable episode signal — MSE reuses ONE `<video>` and fires NO `loadstart` on episode change, so URL, not media events, is authoritative for SPA advances); `reduceMediaSwap(urlMovieId)` is URL-ANCHORED so a browse-preview `<video>` `loadstart` (URL has no `/watch/` id) can never adopt anything — the pollution the old "adopt whatever's pending on ANY swap" allowed; `reduceWatchLeft()` clears `active` + `latestPayload` when the URL leaves `/watch/`. A next-episode prefetch buffered mid-episode is HELD, adopted only when its `/watch/` URL is actually named (the Frieren mid-episode trap stays covered — verified no-regression). `netflix.content.tsx` posts `watch-changed`/`watch-left` on `wxt:locationchange`; `durationchange` is logged-only (fires mid-stream under MSE). IDENTITY GUARDS on BOTH the MAIN re-emit (`handleRequestTracklist`, cached vs `/watch/` id) AND the ISO latched-replay (`subscribeToCaptions` via `platform.currentVideoId()`) stop any stale tracklist reaching a fresh overlay; ISO also dedups by payload identity (videoId+status+track ids+**signed URLs** — a fresh re-parse re-resolves, a byte-identical re-emit skips) and self-heals an aged-held-URL 403 via `activeFromHeld` (one URL-refreshing re-post on the next fresh parse). NOT Chrome-specific — same code, timing-dependent, showed different symptoms per browser. Rich `#seq t=ms`-stamped `[Loom NFLX MAIN]` / `[Loom ISO]` dev-only devlogs reconstruct the full sequence offline.
- **(2026-07-18) A NEW ISO→MAIN postMessage type needs a PAGE reload to take effect on an already-open tab.** The window `message` listener is registered ONCE by the first MAIN generation (the reload-safe holder); a re-injection (extension update/reload) only swaps the HANDLER functions, NOT the listener's dispatch `switch`. So a message type added since the running generation loaded (e.g. `watch-left`, new this build) is silently dropped until a hard reload. When adding an ISO→MAIN type: add the dispatch branch AND tell testers to F5 the tab. (`entrypoints/netflix-main.content.ts` message listener + `LoomMainHolder`.)
- **(0.2.1) Top defaults to the title's AUDIO language on Netflix.** `serializeTrack` stamps `CaptionTrack.audioLangCode` from the manifest's audio-track array (read defensively — `audio_tracks`/`audioTracks`, prefer a native/original-flagged track else the first); `auto-pick.ts::pickTarget` prefers a foreign track whose base lang matches it OVER tier order. Verified live picking ja on Frieren and the spoken language on ko/zh titles. Undefined `audioLangCode` (e.g. YouTube, or a manifest with no recognizable audio array) → falls back to tier ordering, no regression.

**Extension — Prime + positional layout (`apps/extension`, 2026-07):**
- **Prime mounts via a STATE-BASED reconciler, NOT `autoMount`.** Prime's player goes 0×0→full-size via a LAYOUT change, not a DOM mutation, so `autoMount`'s mutation observer never re-fires and the overlay stays stuck on the 0×0 preview. `ensureMount` (`entrypoints/prime.content.tsx`) reconciles to the resolved surface on mutations + a 1 s backstop, migrating on surface **identity** change. (WeTV uses the same pattern for a different reason — player-subtree rebuilds.)
- **The real Prime surface is the LONGEST-DURATION one, not the largest.** Prime autoplays a ~30 s preview trailer on a full-size surface while the real ~6000 s episode loads on a SEPARATE, near-identically-sized surface — "largest area" can't tell them apart (within ~5%). `resolvePrimePlayerSurface` picks by video `duration` (pause-invariant); the overlay anchor AND the playhead binding both go through it so they can't land on different `<video>`s. (`lib/overlay/prime-player-anchor.ts`.)
- **Per-platform prefs MUST be read from a module-level cache, not React state alone.** Prime's reconciler rebuilds the `CaptionProvider` 2–3× on a close→reopen (preview→episode migration); a fresh mount at defaults racing the async `storage.local.get` made per-platform size/position settings *appear* not to persist. The prefs live in module-scope `cachedSizeByPlatform` / `cachedPositionByPlatform`, read synchronously in the `useState` initializers so every rebuild restores instantly (`components/caption-context.tsx`). Storage stays the durable source of truth. Corollary: any NEW persisted key must be added to BOTH the setter AND the load-time `storage.local.get([...])` list — position keys were once written but not read, so they silently never round-tripped.
- **Positional / vertical extras render through `zoneAnchor`, kept SEPARATE from the layout function (`zoneStyle`).** `pickPrimary` (`stream.ts`) chooses the horizontal-bottom cue for the main slot and returns null if none exists (a lone vertical cue must NOT be promoted into the horizontal slot + romaji line). Concurrent non-default-zone cues render via `PositionalCue` at their own zone + writing-mode (furigana only). This separation is load-bearing: the per-platform vertical-nudge sliders adjust `zoneStyle` (main lines only) and MUST NOT move signs / vertical cues off their source position. `CueLayout` only ever comes from a DEFINED region/cue-setting (Prime TTML regions `横下`/`縦右`; Netflix `line:`/`vertical:`), never inferred.
- **`usePlayerScale` measures the visible VIDEO PICTURE, not the player root.** Font sizes scale by `pictureHeight/1080` where picture height = the `<video>`'s `object-fit: contain` box (`min(clientH, clientW·intrinsicH/intrinsicW)`). Measuring the player container instead is why Netflix/YT/WeTV/iQIYI rendered oversized vs Prime (whose anchor already IS the video box). Don't revert it to `playerRoot.clientHeight`. (`lib/overlay/player-scale.ts`.)

**Web app (`apps/web`) — ffmpeg.wasm:**
- The `FFmpeg` class has no `worker.onerror` listener: a worker that fails to boot hangs `load()` forever. Hardened via `FFmpegClient.#init` → `withTimeout` + window-level error capture on the test page.
- `classWorkerURL` MUST be a fully-qualified URL with origin (`${window.location.origin}/ffmpeg`); a path-only string resolves against `import.meta.url` → `file://` and the browser blocks it.
- ffmpeg-core MUST be the ESM build (`@ffmpeg/core/dist/esm/`); the module worker does `(await import(coreURL)).default`. UMD has no default export and silently hangs `load()`.
- TS `moduleResolution: bundler` doesn't map `.js` → `.ts` for value imports — drop the `.js` suffix on imports across `apps/web/lib/`.
- **General rule** (`feedback_async_hang_prevention.md`): every promise from third-party code goes through `withTimeout()` with a labeled rejection. Silent hangs are a banned bug class.

**Desktop (`apps/desktop`):**
- **Dev-mode fonts:** Tauri 2's `resource_dir()` in dev returns the build-artifact dir, not `src-tauri/resources/`; during `npm run tauri dev` set `LOOM_FONT_DIR=$PWD/apps/desktop/src-tauri/resources/fonts` manually. Production bundles read the real resource dir.

---

## Language Pipelines

Implementation lives in `loom_core/romanize.py` + `loom_core/language.py`. Read those for details — this section captures non-obvious gotchas only.

**Japanese:** `_make_japanese_pipeline()` returns `(resolve_spans, spans_to_romaji)` with closure state (`_romaji_meta` carries merge_mask + particle_ha across calls). fugashi (MeCab) + unidic-lite. Three-tier furigana: author inline `kanji(hiragana)` → pre-existing ASS furigana → MeCab fallback. Three long vowel modes: macrons (default) / doubled / unmarked. POS-aware verb chain merging via `_should_merge_for_romaji()`. Particle は → wa via `pos1=助詞, pos2=係助詞`.

**Chinese:** Three variants — `zh-Hans/zh-CN/chs/zh` → Pinyin, `zh-Hant/zh-TW/cht` → Zhuyin, `yue/zh-yue/CantoCaptions` → Jyutping. `_make_pinyin_romanizer()` uses `jieba.cut()` for word boundaries; Traditional → Simplified via OpenCC `t2s` for jieba (Simplified-oriented dict), boundaries mapped back to Traditional for pypinyin. CJK punctuation stripping via `_is_cjk_punct()` filters punctuation-only segments (covers U+3000–U+303F, fullwidth U+FF00–U+FF65, etc.).

**Korean:** `korean-romanizer` (Revised Romanization). Per-syllable annotation gives base reading per char (lookup aid); the romanization line uses full-word `Romanizer(text)` which captures liaison/tensification/nasalization (reading aid). Two layers, two purposes — by design.

**Cyrillic:** `cyrtranslit`. `_CYRILLIC_LANG_CODES` maps BCP-47 → cyrtranslit codes (ru, uk/ua, be/by, sr, bg, mk, mn). Ukrainian/Belarusian disambiguation via `_UKRAINIAN_UNIQUE`/`_BELARUSIAN_UNIQUE` frozensets.

**Thai:** `pythainlp`. 3 phonetic systems: `paiboon` (default, with tone diacritics, vowel remapping ae→ɛ ue→ɯ), `rtgs` (no tones, ASCII), `ipa`. Hybrid tokenizer `_thai_tokenize()`: `word_tokenize(engine='newmm')` → `syllable_tokenize()` on tokens >6 Thai chars. **Critical:** `royin` engine deprecated — mangles consonant clusters; all RTGS/Paiboon+ paths use `thai2rom`. Word boundaries via U+2009 thin space. `annotation_default_enabled: False`.

**Indic (R5-2/R5-3):** Six languages via `aksharamukha.transliterate.process(script, 'IAST', text)` — `_INDIC_SCRIPTS = {hi: Devanagari, bn: Bengali, ta: Tamil, te: Telugu, gu: Gujarati, pa: Gurmukhi}`. Aksharamukha preferred over `indic-transliteration`/sanscript because sanscript distorts Tamil ("vaṇakkam" → "vaṇaghghaṃ") by treating it as Sanskrit-subset. Aksharamukha auto-converts danda (।) and double-danda (॥) to ASCII periods. Per-akshara annotation: `_split_brahmic_aksharas()` accumulates consonant clusters across virama boundaries — runs aksharamukha per-akshara to get correct conjunct readings (Tamil க்க → "kka") that only whole-unit gives. Bengali Khanda Ta (U+09CE) classified as extender, not standalone — acceptable for reading fidelity. `has_phonetic_layer=True`, `supports_ass_annotation=False`.

**Hebrew (R5-4 phase a):** `_make_hebrew_romanizer()` is consonantal transliteration with two heuristics: (1) mater lectionis — ו/י are consonantal (v/y) at word-start or after vowel-letter, vocalic (o/i) after consonant; (2) default 'a' inserted between consecutive consonants. Strips nikud/cantillation (U+0591–U+05C7). Begadkefat (ב כ פ) defaults to soft form (v/kh/f) since unpointed Modern Hebrew has no dagesh marker. **Documented failure modes:** ברוך → varokh not baruch, חברים → chavarim not chaverim. Tests lock these in so a future nikud/dictionary-based pass shows up as test diff.

**Arabic / Persian / Urdu (R5-4 remaining):** `_make_arabic_romanizer` / `_make_persian_romanizer` / `_make_urdu_romanizer` share `_arabic_script_romanize_word()` walker. Same mater-lectionis rule as Hebrew on و/ي (vocalic ū/ī after consonant, consonantal w/y at word-start or after vowel-letter). Strips tashkil before transliteration (subtitle text rarely carries it). Three phonetic systems per language (Duolingo-to-academic hybrid):
- **Arabic** — `learner` (default; emphatics ṣ ḍ ṭ ẓ ḥ + long ā ī ū + ʿ/ʾ + digraphs sh/gh/th/dh/kh) / `din` (full DIN 31635: š ġ ṯ ḏ ḫ) / `loose` (ASCII-only, drops emphatic marks + ayn). Definite article ال handles sun-letter assimilation (14 sun letters double the following consonant: الشمس → ash-shams; 14 moon letters keep al-: القمر → al-qamar). Final ة (tāʾ marbūṭa) → pause-form "a". Alif maksūra (ى) → long ā.
- **Persian** — `learner` (default) / `dmg` (single-char digraph alternatives č ž š ġ ṯ ḏ ḫ). Persian-specific letters پ چ ژ گ. Persian uses Arabic script but ezāfe + vowel inventory differ; emphatic marks are typically collapsed (Persian-style) even in the learner default.
- **Urdu** — `learner` (default) / `ala-lc` (scholarly: candrabindu n̐ for nun-ghunnah, macron ē for yeh-barree). Layers on Persian + retroflexes ٹ ڈ ڑ → ṭ ḍ ṛ + nun-ghunnah ں + yeh-barree ے + aspiration marker ھ (heh doachashmee combines with preceding consonant: بھ → bh, ٹھ → ṭh).
- **Documented failure modes** locked in tests: unvocalized short vowels guessed as 'a' (yaktub → yaktab); no sun-letter assimilation outside ال; Pākistān → Pākasatān (default-'a' between k-s).

**RTL rendering (R5-4 phase b):** `is_rtl_text(text, threshold=0.4)` classifies as RTL when Hebrew/Arabic/Syriac/NKo/Samaritan/presentation-form codepoints > 40% of non-whitespace non-digit. `_build_fullframe_html(top_rtl, bottom_rtl)` injects `dir="rtl"` on relevant `#top`/`#bottom` divs; `unicode-bidi: isolate` on every `.layer` so directionality can't leak. Romanized never gets `dir="rtl"`. `generate_pgs_file()` derives: `top_rtl` from target `lang_cfg['rtl']` (authoritative), `bottom_rtl` from content scan over native events (covers arbitrary user languages without needing a `native_lang_code` API param). `.ass` path untouched — libass handles bidi internally.

**Universal romanization polish** (`_polish_romaji(text, *, capitalize=True)`): runs at every romanizer factory tail. Three passes — fullwidth CJK punctuation → ASCII via `_CJK_TO_LATIN_PUNCT` translate table; strip `\s+` before closing punctuation; uppercase line-start + first alpha after `.!?` when `capitalize=True`. **Capitalize disabled** for Cyrillic (cyrtranslit preserves source case) and Thai (no caps convention). Idempotent.

**Language detection** (`_dominant_script()`): script-specific paths — CJK via `_refine_cjk_detection()`, Cyrillic via `_detect_by_script_chars()` unique-char pre-detection → langdetect fallback, Thai/Indic by script directly, Latin via `_normalize_metadata_lang()` metadata preference over langdetect (fixes Romance language misidentification). Indic scripts mapped 1:1 via `_INDIC_SCRIPT_TO_CODE`. Hebrew detection: `_dominant_script() == 'Hebrew'` → 'he'. Arabic-script detection: when `_dominant_script() == 'Arabic'`, trust langdetect's raw_code if it's `ar`/`fa`/`ur`; otherwise default to `ar`. (No unique-letter pre-detection like Cyrillic — Persian-only letters پ چ ژ گ and Urdu-only ٹ ڈ ڑ ں ے ھ exist, but langdetect was found reliable enough that adding override logic was deferred.)

**Font validation (R6b-fonts):** `loom_core/fonts.py` — `FontScanner` walks one or more font directories, indexes every TTF/OTF/TTC face via `fontTools.ttLib.TTFont`, builds `family → (path, ttc_index)` + per-face cmap maps. Reads `name` records 16/1/4 (typographic family / family / full name, prioritised, Windows-Unicode platform preferred over Mac Roman) plus OS/2 `usWeightClass` so `resolve()` returns Regular weight when multiple weights of the same family are indexed. Mtime-based lazy rebuild; thread-safe. `validate_font(font_name, *, lang_code=None, text=None, scanner=None)` → `FontValidation` (resolved_path, resolved_family, resolved_index, is_fallback, coverage_ok, missing_chars, warnings). Per-language samples in `_LANG_COVERAGE_SAMPLES` (zh-Hans uses 国, zh-Hant uses 國). Module-level `get_default_scanner()` consults `LOOM_FONT_DIR` (`os.pathsep`-separated) then falls back to platform-conventional system font dirs; `set_default_scanner()` for tests / Tauri startup wiring. **`is_fallback=True` semantics in the new backend** = "requested family not in any scanned dir" (the renderer will pick a system / engine fallback at draw time). UI integration deferred.

---

## Style System (R6a)

Per-layer controls (Bottom, Top, Romanized, Annotation): color, opacity, font size, font family, outline (toggle + thickness + color + opacity), shadow (toggle + distance, default 1.5), glow (radius 1–20, color, `\blur` ASS tag). "Top Stack Position": vertical offset (-100 to +100px), `annotation_gap` (-20 to +40px, default 2), `romanized_gap` (-20 to +40px, default 0). These are top-level ints in `styles` dict — `isinstance(config, dict)` guards skip them.

Default font sizes (1080-scale): Bottom=48, Top=52, Romanized=30, Annotation=22. Default outline: Bottom=3.0, Top=2.5, Romanized=1.5, Annotation=1.0.

`_hex_to_ass_color()` / `_ass_color_to_hex()` bridge `#RRGGBB` ↔ `pysubs2.Color`. ASS alpha inverted: `int((1 - opacity/100) * 255)`.

**Gap CSS:** `annotation_gap` uses `transform: translateY()` (not `margin-bottom` — broken in Chromium ruby layout) in preview/rasterize. ASS path uses `\pos()` Y-coordinate math.

---

## Style Mapping

`detect_ass_styles()`: two-pass — pattern match is final, not overridable by event count. Priority: (1) `_PRESERVE_PATTERNS` → preserve, (2) `_EXCLUDE_PATTERNS` → exclude, (3) literal "Dialogue"/"Default" → dialogue (`_DIALOGUE_NAME_RE`), (4) 0 events → exclude, (5) remaining → most-events = dialogue. OP/ED/song/karaoke patterns are preserved (not excluded).

`_iter_dialogue_events()`: selects layer with most non-drawing events (not highest-numbered). Excludes all non-main layers. Yields ALL events in the main layer including overlapping ones — concurrent merging is downstream.

`has_animation` detection per style. `_strip_animation_tags()` for PGS path strips `\k`, `\t()`, `\move()`, `\fad()`; preserves visual tags. `.ass` path passes all tags through.

`_dedup_preserved_for_pgs()`: groups by style + time overlap + text content (substring match). Keeps lowest non-drawing layer. Prevents garbled karaoke layer overlap in PGS.

---

## Test Corpus

| File | Languages | Purpose |
|------|-----------|---------|
| AoT S1E01 MKV | Taiwan CHT, CantoCaptions, Japanese, English | All three Chinese variants + Japanese |
| Three Body S01E01 KONTRAST | Simplified Chinese | Clean Mandarin |
| Three Body S01E01 AMZN | Simplified Chinese | HTML `<font>` tag edge case |
| Seven Samurai 4K MKV (94GB) | Japanese PGS, Trad Chinese, English ×2, Danish, Finnish, Norwegian, Italian, French PGS, German PGS | Large file perf, PGS OCR, European R4 |
| Inuyasha EP028 | Japanese DVD fansub | Legacy subtitle formatting |
| Death Whisperer 3 (non-MKV) | Thai, English (external SRT) | Non-MKV input, external subtitle upload, Thai R4 |

---

## How to Resume

1. `cd` into repo, run `claude`
2. Read this file — it is the authoritative state document
3. Forward-looking plans (v1.5 / v2 / long-term backlog, Owner Auth Tiers B/C, the PGS-in-browser spike verdict) live in `ROADMAP.md`
4. Full session-by-session implementation history lives in the dated archives at `/home/connor/Documents/projects/general_project_notes/notes/srtstitcher/CLAUDE*_ARCHIVE.md`
