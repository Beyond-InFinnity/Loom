"""loom_core — pure subtitle / language / rendering engine.

No Streamlit, no UI. Consumed by:
  - the legacy Streamlit app (``loom_app.py``) during the transition
  - the FastAPI service in ``loom_api/`` (added in step 2)
  - tests in ``tests/``
"""
